# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: edges/resilience.spec.ts >> Ф.4.1 network errors >> 401 mid-session triggers logout
- Location: e2e/edges/resilience.spec.ts:19:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: 'Войти', exact: true })
Expected: visible
Error: strict mode violation: getByRole('button', { name: 'Войти', exact: true }) resolved to 2 elements:
    1) <button type="button" aria-pressed="true">Войти</button> aka getByRole('button', { name: 'Войти' }).first()
    2) <button class="btn" type="submit">Войти</button> aka locator('form').getByRole('button', { name: 'Войти' })

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('button', { name: 'Войти', exact: true })

```

# Page snapshot

```yaml
- generic [ref=e4]:
  - generic [ref=e5]:
    - generic [ref=e6]: 🚀
    - generic [ref=e7]: OXSAR
    - generic [ref=e8]: космическая стратегия
  - generic [ref=e9]:
    - generic [ref=e10]:
      - button "Войти" [pressed] [ref=e11] [cursor=pointer]
      - button "Регистрация" [ref=e12] [cursor=pointer]
    - generic [ref=e13]:
      - generic [ref=e14]:
        - generic [ref=e15]: E-Mail или логин
        - textbox "E-Mail или логин" [ref=e16]:
          - /placeholder: example@mail.com или alice
      - generic [ref=e17]:
        - generic [ref=e18]: Пароль
        - textbox "Пароль" [ref=e19]:
          - /placeholder: минимум 8 символов
      - button "Войти" [ref=e20] [cursor=pointer]
```

# Test source

```ts
  1  | // Ф.4.1–4.2: сетевые ошибки и пустые состояния.
  2  | 
  3  | import { test, expect } from '@playwright/test';
  4  | import { loginAs } from '../fixtures/auth';
  5  | import { goToTab } from '../helpers/nav';
  6  | 
  7  | test.describe('Ф.4.1 network errors', () => {
  8  |   test('500 from server — user sees something, not a white screen', async ({ page }) => {
  9  |     await loginAs(page, 'bob');
  10 |     // Интерсепт конкретный endpoint
  11 |     await page.route('**/api/planets/*/buildings', (route) =>
  12 |       route.fulfill({ status: 500, body: JSON.stringify({ error: { code: 'internal', message: 'oops' } }) }),
  13 |     );
  14 |     await goToTab(page, 'buildings');
  15 |     // main должен быть виден, даже если данные не пришли
  16 |     await expect(page.locator('main.ox-content')).toBeVisible();
  17 |   });
  18 | 
  19 |   test('401 mid-session triggers logout', async ({ page }) => {
  20 |     await loginAs(page, 'bob');
  21 |     await page.route('**/api/planets', (route) =>
  22 |       route.fulfill({
  23 |         status: 401,
  24 |         body: JSON.stringify({ error: { code: 'unauthorized', message: 'expired' } }),
  25 |       }),
  26 |     );
  27 |     // Любой обновляющий запрос вызовет 401 → логаут
  28 |     await page.goto('/');
> 29 |     await expect(page.getByRole('button', { name: 'Войти', exact: true })).toBeVisible({ timeout: 10_000 });
     |                                                                            ^ Error: expect(locator).toBeVisible() failed
  30 |   });
  31 | });
  32 | 
  33 | test.describe('Ф.4.2 empty states', () => {
  34 |   test('alice: inbox is not empty (has welcome), but achievements likely empty', async ({ page }) => {
  35 |     await loginAs(page, 'alice');
  36 |     await goToTab(page, 'achievements');
  37 |     await expect(page.locator('main.ox-content')).toBeVisible();
  38 |   });
  39 | 
  40 |   test('alice: no artefacts — empty state is friendly', async ({ page }) => {
  41 |     await loginAs(page, 'alice');
  42 |     await goToTab(page, 'artefacts');
  43 |     await expect(page.locator('main.ox-content')).toBeVisible();
  44 |     // Не падает и не показывает "undefined"
  45 |     await expect(page.locator('main.ox-content')).not.toContainText(/undefined|null|NaN/);
  46 |   });
  47 | });
  48 | 
```