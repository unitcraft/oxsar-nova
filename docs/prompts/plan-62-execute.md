# Промпт: выполнить план 62 (feasibility/audit ремастера origin на nova-backend)

**Дата создания**: 2026-04-28
**План**: [docs/plans/62-origin-on-nova-feasibility.md](../plans/62-origin-on-nova-feasibility.md)
**Применение**: вставить блок ниже в новую сессию Claude Code в
рабочей директории `d:\Sources\oxsar-nova`. Агент прочитает план 62
самостоятельно и выполнит исследование с финализацией в
`docs/research/origin-vs-nova/`.

---

```
Задача: выполнить план 62 — feasibility/audit-исследование game-nova
как backend для game-origin (ремастер).

ВАЖНОЕ:
- Это ИССЛЕДОВАТЕЛЬСКИЙ план, КОДА НЕ ПИШЕМ. Только чтение, анализ
  и Markdown-файлы в docs/research/origin-vs-nova/.
- Стратегия (ремастер на nova-backend, pixel-perfect клон UI) уже
  принята пользователем. НЕ переоценивай feasibility, выдай
  список работ и декомпозицию в будущие планы.

ПЕРЕД НАЧАЛОМ:

1) git status --short — если есть чужие изменения от параллельных
   сессий, спроси пользователя. Бери только свои файлы.

2) Прочитай ПОЛНОСТЬЮ:
   - docs/plans/62-origin-on-nova-feasibility.md (твоё ТЗ —
     соблюдай каждый пункт чек-листа Ф.1-Ф.6 и пороги качества)
   - CLAUDE.md (правила проекта: коммиты, языки, hooks)
   - docs/balance/analysis.md (готовый анализ балансных формул
     game-nova — НЕ дублируй, ссылайся)
   - docs/status.md (матрица готовности модулей nova)

3) Прочитай выборочно по мере необходимости:
   - docs/plans/15-alien-holding-thursday.md (категория 4 — alien-AI)
   - docs/plans/20-legacy-port.md (что портировано из legacy)
   - docs/plans/57-mail-service.md (TipTap как редактор)
   - docs/legacy/game-origin-access.md — **запущенный game-origin**
     на http://localhost:8092/. Dev-логин:
       curl -s -c /tmp/origin-jar.txt -L \
         "http://localhost:8092/dev-login.php" -o /dev/null -w "%{http_code}\n"
     После — любой экран:
       curl -s -b /tmp/origin-jar.txt \
         "http://localhost:8092/game.php?go=Main" -o /tmp/x.html \
         -w "%{http_code} %{size_download}\n"
     БД-запросы:
       docker exec docker-mysql-1 mysql -uoxsar_user -poxsar_pass oxsar_db \
         -e "SELECT ... FROM na_... ;"
     Это **основной** источник для сверки UI/поведения origin.
   - docs/legacy/game-reference.md — legacy oxsar2 на localhost:8080
     (вторичный источник, если нужна сверка оригинального PHP).

ЧТО НУЖНО СДЕЛАТЬ:

Создать директорию docs/research/origin-vs-nova/ и в ней файлы
по структуре из плана 62 (раздел "Куда сохраняется результат"):

  origin-inventory.md       — инвентарь origin (Ф.1)
  nova-inventory.md         — инвентарь nova   (Ф.2)
  comparison.md             — 8 сравнительных таблиц (Ф.3)
  divergence-log.md         — журнал расхождений D-NNN (Ф.3.5)
  nova-ui-backlog.md        — UI-функции (U-NNN) и UX-микрологика
                              (X-NNN), отсутствующие в nova
  origin-ui-replication.md  — pixel-perfect инвентарь S-NNN
  alien-ai-comparison.md    — углублённое сравнение AI инопланетян
  formula-dsl.md            — DSL legacy-формул из БД
  roadmap-report.md         — итоговая декомпозиция в будущие планы
                              (Ф.4 + Ф.5)
  screenshots/              — опционально, эталоны для diff-теста

ПОРОГИ КАЧЕСТВА (обязательные, не "пожелания"):

- divergence-log.md: ≥ 30 записей D-NNN. Каждая по шаблону из плана
  62 (file:line обеих сторон + конкретные числа + предложение
  правки + оценка объёма + риски). Если < 30 — продолжай искать,
  ты схалтурил.
- nova-ui-backlog.md: ≥ 10 записей U-NNN + ≥ 20 X-NNN.
- origin-ui-replication.md: ВСЕ 55 контроллеров из
  projects/game-origin/src/game/page/ должны быть сопоставлены
  с экранами S-NNN. Пропуск = баг.
- Все 🟣-записи (тихая семантика — одинаковые имена, разная
  семантика) должны иметь конкретный пример расходящегося
  поведения, не "возможно отличается".
- Каждое D-NNN/U-NNN/X-NNN/S-NNN с уникальным ID для ссылок из
  будущих планов.

КЛЮЧЕВЫЕ АРХИТЕКТУРНЫЕ ВВОДНЫЕ (зафиксированы в плане 62, прочти и
применяй):

1. Cross-universe сервисы (identity, billing, portal, reports, mail,
   рефералка, admin-bff, moderation) — ЕДИНЫЕ. Их в журнал НЕ
   записываем как расхождения.

2. Origin-фронт = pixel-perfect клон с game-origin (тема standard),
   стек React/TS/TanStack Query/Zustand/TipTap по nova-конвенциям.
   Только русский язык. JS-логику legacy НЕ переносим, только
   воссоздаём поведение средствами React. Шрифты/иконки берём из
   game-origin (с проверкой лицензий).

3. Origin-фронт сразу пишется на nova-API, без backend-адаптеров.
   Расхождения в именах путей в журнал НЕ записываем.

4. **БАЛАНС NOVA НЕ МЕНЯЕТСЯ.** Для современных вселенных
   (uni01/uni02) истина — текущий nova (YAML + Go-код, ребалансы
   планов 17/18/20/21). Для legacy01 — game-origin (na_construction
   + consts.php). Расхождения = "параметризовать в nova через
   legacy.yaml/код-путь", а НЕ "исправить число в nova". Каждое
   D-NNN формулируется как "какой ключ конфигурации/код-путь
   нужен", а не "вернуть к origin".

5. "Общий знаменатель" — только для отсутствующих механик (биржа,
   турниры, телепорт планеты, расширенная дипломатия и т.п.) →
   реализуем в nova-backend. Числа/формулы — НЕ приводим к общему,
   выносим в конфиг.

6. Mail-service (план 57) идёт ДО origin-фронта. TipTap уже будет.
   Чат origin (chat.tpl + bbcode) — выкидывается, не портируется.
   В журнал не идёт.

7. game-origin = истина для legacy. game-origin/ (clean-room) ПЛЮС
   d:\Sources\oxsar2\ (с www/ext/) — оба считаем legacy при сверке
   формул. Расхождения origin ↔ ext/ отмечать отдельно если найдёшь.

8. БД-таблица na_construction содержит формулы как строки в полях
   prod_*, cons_*, charge_* (varbinary(255)). Найди PHP-парсер
   этих строк (в Functions.inc.php или production-классах) — это
   твой DSL, опиши в formula-dsl.md.

9. Известный пример runtime-генерируемого ассета:
   projects/game-origin/public/artefact-image.php (PHP-GD,
   композитная PNG артефакта). Найди другие через grep по
   imagecreate|imagecopy|imagepng|imagejpeg|imagettftext.

10. Альянсная подсистема в origin богата (1707 строк, ~30 действий
    в Alliance.class.php + AllianceList.class.php). Проверь детально
    по списку из плана 62, секция "Особый блок: Альянсы".

11. Event-loop: в origin 51 тип событий, в nova ~41. Стартовая
    карта расхождений уже частично в плане 62 (Категория 4).
    Углубляй и верифицируй. Особое внимание AlienAI.class.php
    (1127 строк) — отдельный артефакт alien-ai-comparison.md
    с state machine и переходами.

12. UX-микрологика: цвета, disabled-состояния, tooltip'ы с
    причинами — самая ценная UX-память legacy. Прочёсывай .tpl
    на предмет {if $cost > $have}красный{/if} и подобного.
    Идёт в nova-ui-backlog.md как X-NNN.

ПОРЯДОК ВЫПОЛНЕНИЯ:

Иди строго по фазам Ф.1 → Ф.6 из плана 62. После каждой
ключевой фазы — короткий отчёт пользователю (3-5 предложений:
что сделано, какие неожиданности, идём дальше).

ПРОМЕЖУТОЧНЫЕ КОММИТЫ:

Делай коммиты после Ф.1, Ф.2, Ф.3 — чтобы не потерять работу
при сбое и чтобы пользователь мог проверить. Каждый коммит:
- conventional: docs(research): план 62 Ф.X — ...
- поимённый git add (только свои файлы — параллельные сессии!)
- trailer: Generated-with: Claude Code (НЕ Co-Authored-By —
  git hook уберёт автоматически, см. CLAUDE.md Onboarding)

ВАЖНОЕ: GIT-ИЗОЛЯЦИЯ ОТ ПАРАЛЛЕЛЬНЫХ СЕССИЙ:

- git status --short ПЕРЕД каждым git add
- git add ТОЛЬКО конкретные пути (docs/research/origin-vs-nova/...,
  docs/plans/62-..., docs/project-creation.txt, docs/release-roadmap.md)
- git status --short ПЕРЕД каждым git commit (убедись что не
  захватил чужое)
- Если случайно staged чужой файл: git reset HEAD -- путь
- НИКОГДА не делай git add . или git add -A

ФИНАЛИЗАЦИЯ (Ф.6):

1. Запись в docs/project-creation.txt — итерация 62 (хронология,
   не дублируй сам план).
2. Обновить docs/release-roadmap.md — добавить дорожную карту
   ремастера (последовательность будущих планов из roadmap-report.md
   с зависимостями).
3. Обновить шапку плана 62 — статус "Завершён <дата>".
4. Финальный коммит: docs(plans,research): план 62 финализация
   с перечислением всех созданных артефактов.

ЧЕГО НЕ ДЕЛАТЬ:

- Не писать НИКАКОЙ код (Go, TS, SQL, YAML с балансом и т.п.).
- Не менять game-nova и game-origin (не правь их файлы вообще).
- Не делать поверхностный анализ — пороги качества обязательны.
- Не дублировать docs/balance/analysis.md — ссылайся.
- Не записывать в журнал:
  · тривиальные расхождения именования (user_id vs userid)
  · разные пути API при той же семантике
  · cross-universe сервисы (identity/billing/portal/etc.)
  · BBCode-логику чата (выкидывается)
- Не писать "возможно", "вероятно", "обычно" — каждое расхождение
  должно быть верифицировано в коде с file:line. Если не уверен —
  вернись и проверь.
- Не делать выводы "не подходит" / "infeasible" — стратегия
  принята, выдаёшь roadmap.

ОЦЕНКА ОБЪЁМА:

4-6 рабочих дней агента в активном темпе. Если идёт быстрее —
проверь пороги качества, скорее всего что-то пропущено. Если
медленнее — разбей на больше промежуточных коммитов, не теряй
работу.

ОТЧИТЫВАЙСЯ ПОЛЬЗОВАТЕЛЮ ПЕРИОДИЧЕСКИ:

- После Ф.1 (инвентарь origin готов).
- После Ф.2 (инвентарь nova готов).
- После Ф.3 (8 сравнительных таблиц + начало D-NNN).
- После Ф.3.5 (журнал D-NNN ≥30 записей).
- После UI-backlog и replication-инвентаря.
- После roadmap-report.md.
- Финал.

При промежуточных отчётах коротко и по делу: "Сделано X. Найдено Y
неожиданного. Идём дальше". Не пиши длинные сводки на каждом шаге.

УСПЕШНЫЙ ИСХОД ВЫГЛЯДИТ ТАК:

После закрытия плана 62 пользователь может запустить любой
будущий план словами "делай по записи D-042" или "реализуй
экран S-015", и агент-реализатор соберёт всё нужное из артефактов
без повторного исследования.

Стартуй.
```
