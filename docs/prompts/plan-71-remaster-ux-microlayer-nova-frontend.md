# Промпт: выполнить план 71 (UX-микрологика → nova-frontend)

**Дата создания**: 2026-04-28
**План**: [docs/plans/71-remaster-ux-microlayer-nova-frontend.md](../plans/71-remaster-ux-microlayer-nova-frontend.md)
**Зависимости**: нет критичных.
**Объём**: 2-3 нед, ~600-1200 строк frontend.

---

```
Задача: выполнить план 71 (ремастер) — применить X-NNN записи
(UX-микрологика origin) на nova-frontend для всех вселенных.

ВАЖНОЕ:
- Применимо к ВСЕМ вселенным (не только origin) — это «общий
  знаменатель» накопленного UX-опыта legacy-проекта.
- Можно делать параллельно с другими планами серии.

ПЕРЕД НАЧАЛОМ:

1) git status --short.

2) Прочитай:
   - docs/plans/71-remaster-ux-microlayer-nova-frontend.md
   - docs/research/origin-vs-nova/nova-ui-backlog.md X-001..X-022
     (22 записи UX-микрологики)
   - docs/research/origin-vs-nova/roadmap-report.md «Часть I.5»
     (R0-R15)

3) Выборочно:
   - projects/game-nova/frontend/src/features/ — стиль компонентов.
   - projects/game-origin-php/src/templates/standard/ — referenc
     визуала (legacy-PHP, не для копирования).

ЧТО НУЖНО СДЕЛАТЬ:

22 X-NNN записи в priority-order. ⭐ — критичные (первые):

⭐ X-001: Дефицит ресурсов с пометкой `(нужно X)`.
⭐ X-003: Показ требований при `can_build = false` (конкретно
   «не хватает Энергетической технологии lvl 5»).
⭐ X-010: Энергодефицит красным цветом.
X-002: Потребление красным при отрицательном балансе.
X-013: added_level +/- зелёное / красное.
X-021: Счётчик новых ачивок в navbar (с учётом отложенного плана 70 —
   делать как заглушку, активируется когда achievements реактивируются).
X-014: Ремонтные поля в боевом отчёте.
X-007: Нет слотов с подсчётом (X из Y).
X-008: Статус артефактов (активен / истекает).
X-009: Расширенный helptip при наведении.
... остальные 12 X-NNN.

Реализация:
- React-компоненты-helper'ы в frontend/src/components/feedback/:
  ResourceDeficitBadge.tsx, RequirementHint.tsx и т.д.
- Цвета через CSS-переменные nova theme (не хардкод).
- Tooltip'ы — единый компонент Tooltip из components/ui/.
- Никаких новых API — всё на существующих DTO; если данных не
  хватает, расширить DTO (R2 — OpenAPI первым).

ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА:

R0: общий знаменатель UX, не правка чисел/механик.
R2: если расширяешь DTO — OpenAPI первым.
R12: i18n — grep nova-bundle перед созданием новых ключей.
   Большинство строк UX-микрологики могут уже существовать в nova
   (тексты ошибок, описания требований). Цель — максимальное
   переиспользование. В коммитах указывать соотношение
   переиспользовано/новых.
R15: без упрощений — все edge-кейсы покрыты тестами.

GIT-ИЗОЛЯЦИЯ:
- Свои пути: frontend/src/components/feedback/,
  frontend/src/features/ (расширения существующих экранов),
  openapi.yaml (если требуется расширение DTO),
  docs/plans/71-..., nova-ui-backlog.md (X-001..X-022 пометить ✅).

КОММИТЫ:

Можно одним: feat(frontend): UX-микрологика origin → nova (план 71)
ИЛИ разбить по группам экранов (Constructions, Empire, Battle, Chat).

ЧЕГО НЕ ДЕЛАТЬ:
- НЕ дублировать X-логику в origin-фронте плана 72 (он
  pixel-perfect клон визуала, X-логика приходит из nova-API +
  общие компоненты).
- НЕ вводить новые UX-фичи, которых нет в origin — это «забор»
  накопленного, не редизайн.
- НЕ хардкодить строки.

УСПЕШНЫЙ ИСХОД:
- X-001..X-022 закрыты.
- React-компоненты-helper'ы переиспользуемы.
- В коммитах i18n-метрика «переиспользовано/новых».

Стартуй.
```
