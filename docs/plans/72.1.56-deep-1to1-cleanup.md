# 72.1.56 — Deep 1:1 cleanup

**Дата:** 2026-05-03
**Контекст:** После 72.1.55 проведена полная переоценка `docs/simplifications.md`.
Большинство «открытых» записей оказались ложными срабатываниями grep
(закрыты, но без явного маркера) или архитектурными решениями. Реальных
1:1-нарушений по легаси-PHP и Java-движку — 12 штук + косметика.

Этот план их **закрывает 1:1 с источниками**:
- Бой: `d:\Sources\oxsar2-java` (Participant.java, Battle.java) и
  `d:\Sources\oxsar2\www\game\Assault.class.php`.
- Игровая механика: `d:\Sources\oxsar2\www\game\page\*.class.php`,
  `d:\Sources\oxsar2\www\game\AlienAI.class.php`,
  `d:\Sources\oxsar2\www\ext\page\Ext*.class.php`.
- Легаси-PHP rewrite в проекте: `projects/game-legacy-php/src/game/`.

## Группа A — короткие задачи (1-3 ч/каждая)

### A1. `[EXPEDITION] expExtraPlanet` — KindExpirePlanet handler
- Создать новый event-kind `KindExpirePlanet` (cron-by-fire-at).
- При создании временной планеты в `expExtraPlanet` ставить INSERT
  events (kind=ExpirePlanet, fire_at=expires_at).
- Handler: `DELETE FROM planets WHERE id=$1 AND expires_at<=now()`
  + cascade flotов/queues/etc через FK.
- DoD: тест на cleanup expired planet, документация в plan-комменте.

### A2. `[09-Ф2.1] events.trace_id helper`
- Helper: `event.Insert(ctx, tx, kind, userID, planetID, fireAt, payload)`
  читает `trace.FromContext(ctx)` и проставляет `trace_id` автоматически.
- Замена 11 raw `INSERT INTO events` (по результатам `grep "INSERT INTO events"`).
- DoD: после A2 trace_id end-to-end от HTTP до handler'а.

### A3. `[P88.2] Officer push — английский id`
- Добавить блок `officerName: { commander: "Командор", engineer:
  "Инженер", geologist: "Геолог", admiral: "Адмирал", ... }` в
  `configs/i18n/{ru,en}.yml`.
- В `officer/service.go:391,405` резолвить ключ через i18n перед
  подстановкой в `renewedSubject`/`expiredSubject`.
- DoD: push-уведомление содержит русское имя офицера.

### A4. `[Alliance 67 Ф.5.1-5.3]` — frontend permissions доформирование
- 72.1.55 Task D уже сделал `effective_perms` в Member DTO + helper
  `findSelfPerms`. Проверить, что **все** alliance-экраны используют
  `hasPerm()` вместо `isOwner`-shortcut (RanksScreen, ApplicationsScreen,
  AuditScreen, TransferOwnershipScreen).
- DoD: ни один alliance-экран не делает `if (member.isOwner)` для
  скрытия management-кнопок — везде через `hasPerm`.

## Группа B — средние задачи (3-8 ч/каждая)

### B5. `[H.1.7] Phalanx producer + сканер` (1:1 с legacy)
- Изучить `Phalanx.class.php` в legacy-PHP rewrite + оригинал в
  `d:\Sources\oxsar2\www\game\page\Phalanx.class.php`.
- Зеркалить:
  - Здание `phalanx_sensor` (id из legacy) уже в buildings.yml — проверить.
  - Дальность сканирования = `level^2 - 1` систем от планеты с фалангой.
  - Стоимость скана: фиксированный H по формуле legacy.
  - Endpoint `POST /api/galaxy/phalanx-scan` с body `{from_planet_id,
    target_galaxy, target_system, target_position}`.
  - Возвращает `{flights: [{user_id, ships, mission, depart_at,
    arrive_at}], scan_cost_h: int}`.
  - INSERT в messages (folder=11/Фаланга) для атакующего/transit-флотов
    (см. legacy логику кому видно).
- UI: в GalaxyScreen новая кнопка «Сканировать фалангой» при наличии
  здания + ввод координат → отображение результатов.
- DoD: e2e-тест skanа на тестовых полётах, miграция (если новая колонка),
  i18n keys `galaxy.phalanxScan*`.

### B6. `[Repair] частичный quantity 1:1`
- Legacy `ExtRepair.class.php:510`: `$quantity = min($quantity,
  $row["damaged"])` + форма с `<input type='text' name='id' value='0'>`
  на каждый damaged-юнит.
- Backend: `EnqueueRepair(ctx, planetID, userID, items []RepairItem)`
  где `RepairItem{UnitID int, Quantity int}`.
- Валидация `min(quantity, damaged_count)` per-unit.
- Стоимость считается из `quantity`, не из `damaged_count`.
- UI: RepairScreen — number input на каждой строке (default=damaged_count
  как в legacy, но игрок может занизить).
- DoD: тест на «починить 3 из 10 damaged», сохранение остатка.

### B7. `[65-Ф.3] DESTROY_BUILD_RESULT_MIN_OFFS_LEVEL` — эвристика exclude
- Legacy `Assault.class.php:261-263`:
  ```
  $min_result_level = $row["level"] + DESTROY_BUILD_RESULT_MIN_OFFS_LEVEL;
  $new_result_level = $defender_builds[$row["buildingid"]]["level"] - 1;
  if($new_result_level < $min_result_level) unset(...);
  ```
- В `internal/fleet/destroy_building.go::tryDestroyBuilding` перед random-
  choice'ом загружать buildings атакующего/всех ACS-участников и фильтровать
  defender_builds по этому правилу.
- Константу `DESTROY_BUILD_RESULT_MIN_OFFS_LEVEL` положить в
  `configs/balance/origin.yaml` (и DefaultConfig fallback в Go).
- DoD: тест property-based «attacker level=N → защитное здание уровня
  >N+threshold всегда включается, ниже — исключается».

### B8. `[Alien AI] unloadAlienResources` — % от ЗАХВАЧЕННЫХ
- Legacy `AlienAI.class.php:1053-1061`:
  `min(captured*0.7, captured*0.1*times)` по каждому ресурсу,
  `captured` хранится в payload родительского события (HALT/HOLDING).
- В `internal/origin/alien/holding_ai_handler.go` (или
  `internal/alien/holding.go::unloadAlienResources` — выяснить, какой
  активен после Ф.4):
  - Расширить `holdingPayload` полями `CapturedMetal/Silicon/Hydrogen int64`.
  - В `spawnHalt` (после боя) проставлять captured из лоута атаки.
  - В unload — формула из legacy: `min(captured*0.7, captured*0.1*times)`,
    декремент `captured -= unloaded`.
- DoD: тест «alien захватил 1M metal → unload не больше 700k за раз,
  накопительно ≤ 100% от captured».

### B9. `[65-Ф.1] demolish API` — public endpoint + UI
- Legacy: `Constructions.class.php:28-29` GET `?go=DemolishConstruction&demolish=id`.
  Сразу INSERT event KindDemolishConstruction (handler уже есть).
- Backend:
  - `building.Service.Demolish(ctx, userID, planetID, unitID)`:
    1. SELECT building_level (must be > 0).
    2. Расчёт времени демолиша по building catalog (formula легаси:
       обычно build_time/2 — проверить).
    3. INSERT в `construction_queue` запись с `kind='demolish'` (либо
       отдельная table, либо флаг).
    4. INSERT events (KindDemolishConstruction, fire_at=now+duration).
    5. Idempotency-Key поддержка.
  - `POST /api/planets/{id}/demolish` body `{unit_id}`.
- OpenAPI: новый endpoint.
- UI: в ConstructionsScreen кнопка «✗ Снести» рядом с каждым построенным
  зданием. ConfirmDialog (destructive=true). Disabled если уже в очереди.
- DoD: e2e «построил → снёс → уровень -1 → ресурсы НЕ возвращены»
  (legacy не возвращает).

### B10. `[M4.1] Регенерация щитов 100% → partial с затуханием`
- Java `Participant.java::shieldDamageFactor` — partial regen:
  если в раунде N щит сильно пробит, в раунде N+1 регенирация неполная.
- В `internal/battle/engine.go::regen`:
  - В `unitState` добавить `turnShieldMax float64` (хранит max за раунд).
  - При commitDamage: если `turnShield < startTurnShield` → запомнить factor.
  - Regen в начале нового раунда: `turnShield = max(0, turnShieldMax -
    decay)` где decay вычисляется по Java-формуле.
- Прочесть Java-источник: `d:\Sources\oxsar2-java\assault\src\main\java\
  com\oxsar2\assault\battle\Participant.java` (методы processAttack
  shield-блок строки 315-427, найти shieldDamageFactor).
- DoD: golden-сценарий «mass-pierce → следующий раунд regen<100%»,
  сравнение с jar-output (если есть).

### B11. `[M4.x] attacker_* поля юнитов`
- Legacy: na_ship_datasheet имеет parallel-колонки `attacker_front/
  attack/shield/ballistics/masking` отличающиеся от defender-версий
  у Deathstar (front 10→9 в атаке) и Alien Screen (front 15→16).
- В `configs/ships.yml` добавить optional блок `attacker:` в spec:
  ```yaml
  deathstar:
    front: 10
    attacker:
      front: 9
  ```
- В `internal/config/catalog.go::ShipSpec` добавить `Attacker *ShipStats`
  (nil если нет override).
- В `battle.Unit` пробросить (Front/Attack/Shield/Ballistics/Masking уже
  есть; добавить аналоги для роли attacker).
- В `engine.go` при сборке участника боя: если роль = attacker и есть
  `Attacker.*` → используем их.
- DoD: тест «Deathstar в атаке front=9, в защите front=10»; battle-sim
  совпадает с legacy для DS-боёв.

## Группа C — большой план

### C12. `[M4.be_points] use в бою` — отдельный план **72.1.57**
- Scope: UI слайдеры, валидация, формула в engine, тесты battle-sim,
  cancel-refund.
- НЕ делать в этой итерации; создать stub-файл
  `docs/plans/72.1.57-be-points-use.md` с детальным ТЗ для следующей сессии.
- DoD здесь: только stub-план + ссылка из 72.1.56.

## Группа D — косметика (15 минут в конце)

### D13. Маркеры ЗАКРЫТО
В `docs/simplifications.md` дописать в заголовки:
- `[M4.4a] rapidfireToMap (исправлено)` → ` — ЗАКРЫТО`
- `[M4.4a] Debris=часть loot (исправлено)` → ` — ЗАКРЫТО`
- `[M4.2] Ballistics/masking — детерминированная формула без RNG` —
  пояснить что это **соответствие** Java, не упрощение; статус ЗАКРЫТО.

### D14. Переклассификация
- `[Market] Только в рамках одной планеты`: переписать что **legacy ровно
  такой же**, межпланетка — новая фича, не упрощение.
- `[66-Ф.5] alien buyout`: уже есть пометка «новая фича» в теле; добавить
  в заголовок «не упрощение, новая фича game-nova».

## Порядок выполнения

A1 → A2 → A3 → A4 → B6 → B9 → B7 → B8 → B11 → B10 → B5 → C12 → D13 → D14.

Логика порядка:
- A — короткие, быстрый прогресс.
- B6 (repair quantity) — простая, изолированная.
- B9 (demolish API) — изолирован от боя.
- B7/B8 — alien-связанные, делаем парой.
- B11 (attacker_*) — конфиг + engine, без UI.
- B10 (regen) — самое тонкое в бою, делаем после B11 чтобы все battle-
  изменения коммитить вместе.
- B5 (phalanx) — крупная новая фича, в конце чтобы не блокировать остальные.
- C12 — только stub.
- D — финальная зачистка.

## DoD общий

- `make test` зелёный после каждой задачи.
- `make lint` зелёный.
- Каждая задача = отдельный коммит с conventional message
  `feat(...): план 72.1.56 task NN — ...`.
- После каждой задачи: обновить `docs/simplifications.md` (закрыть запись)
  + `docs/project-creation.txt` (один абзац «что сделано»).
- После всех задач: финальный commit с обновлением этого плана (статус
  каждой задачи — ✅).
