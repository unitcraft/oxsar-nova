# План 72.1 (ремастер): Стабилизация dev-стэка после ремастера

**Дата**: 2026-04-29
**Статус**: ✅ Завершён
**Зависимости**: план 72 (Spring 4 ч.2 завершил основную часть ремастера —
после этого впервые поднимался полный dev-стек end-to-end).
**Связанные планы**: 36 Ф.12 (single auth provider — identity, JWKS-only),
52 (`users.role` удалён из game-nova), 53 (admin-консоль).

## Цель

Полный dev-стек (`docker compose -f deploy/docker-compose.yml up`) после
завершения plan'ов ремастера ни разу не запускался end-to-end в полном
составе. При первом фактическом прогоне выявилось 5 разных мелких
рассогласований между сервисами — окружение, имена переменных, удалённый
column, забытый hydrate. Цель плана — **сделать стек дев-готовым:
любой свежий запуск `docker compose up` → все сервисы healthy → залогиниться
в портал/админку/игру тестовым юзером без ручной правки конфигов**.

## Что было сломано (5 классов багов — части 1-5)

### 1. Воркеры падали со старта — нет `IDENTITY_JWKS_URL`

Backend и uni02-backend получали переменную через `IDENTITY_JWKS_URL`,
а у `worker` и `uni02-worker` она не была прописана в compose. После
плана 36 Ф.12 общий config обязательно требует JWKS-URL (HS256 удалён).

**Проявление**: `docker logs deploy-worker-1` →
`ERROR worker exit err="IDENTITY_JWKS_URL is required (legacy AUTH_JWKS_URL also accepted)"`.

**Фикс**: добавил `IDENTITY_JWKS_URL: http://identity-service:9000`
в секции `worker` и `uni02-worker` в `deploy/docker-compose.yml`.

### 2. Vite-фронты не могли проксировать /auth/* — расхождение имён переменных

Compose передавал во все три фронта (nova, uni02-nova, portal)
`VITE_IDENTITY_TARGET=http://identity-service:9000`. При этом
`vite.config.ts` всех трёх фронтов читал переменную **`VITE_AUTH_TARGET`**,
fallback на `http://localhost:9000`. Внутри docker-контейнера `localhost:9000`
указывает на сам контейнер фронта → ECONNREFUSED → vite-proxy отдавал
HTTP 500 на любой запрос `/auth/*`.

**Проявление**: попытка логина на `http://localhost:5174/login` →
HTTP 500, в `docker logs deploy-portal-frontend-1`:
`[vite] http proxy error: /auth/login AggregateError [ECONNREFUSED]`.

**Решение**: переменная должна называться `VITE_IDENTITY_TARGET`
(консистентно с `IDENTITY_JWKS_URL`, `IDENTITY_SERVICE_URL`,
`IDENTITY_URL` — пользовательское решение, см. итеративный диалог в
сессии). Откатил compose; переименовал в трёх `vite.config.ts` и в
`.env.example` origin-фронта.

**Файлы**:
- `projects/portal/frontend/vite.config.ts` (2 occurrence)
- `projects/game-nova/frontends/nova/vite.config.ts` (2 occurrence)
- `projects/game-nova/frontends/origin/vite.config.ts` (2 occurrence)
- `projects/game-nova/frontends/origin/.env.example` (комментарий + пример)

### 3. Шапка портала «Войти» оставалась после успешного логина

`LoginPage` после `setAuth(res); window.location.href='/'` делает
полную перезагрузку. Стор Zustand пересоздаётся с `accessToken` и
`refreshToken` из `localStorage`, но **`user: null`**. `Layout`
показывает «Войти/Регистрация» когда `user === null`.

**Решение**: добавил метод `hydrate()` в стор — если есть
`accessToken`, но нет `user`, дёргает `GET /auth/me` и заполняет
`user`. Битый/просроченный токен — чистит localStorage. Вызов один
раз в `App.tsx` через `useEffect`.

**Файлы**:
- `projects/portal/frontend/src/store/auth.ts` — `hydrate()`.
- `projects/portal/frontend/src/App.tsx` — `useEffect(() => { void hydrate(); }, [])`.

**Гэп при отладке**: на Windows + Docker Desktop volume mount inotify
не передаётся в контейнер vite — фронт продолжал отдавать
закэшированный bundle без `hydrate`. Лечится `docker compose restart
<frontend>` + hard-reload в браузере. Зафиксировал в memory как
feedback-правило (после правок в docker'е верифицировать через
`docker exec grep` + `curl http://<port>/src/...`).

### 4. Game-nova `/api/me` падал 500 на удалённый `users.role`

После плана 52 миграция удалила столбец `users.role` (роли уехали в
identity, доступны через `claims.Roles` в JWT). Но `Me` handler
([backend/internal/auth/handler.go:35](../../projects/game-nova/backend/internal/auth/handler.go))
всё ещё селектил `COALESCE(role::text, '')` из `users` →
`ERROR: column "role" does not exist (SQLSTATE 42703)`.

**Проявление**: при попытке зайти в игру под identity-юзером
(`admin1`) — `EnsureUserMiddleware` создавал запись в game-nova
успешно, потом `/api/me` падал 500 → фронт не мог открыться.

**Решение**: убрал `role` из SQL в `Me`, добавил `roles` в JSON-ответ
из `RSAClaims(r.Context()).Roles`.

**Файл**: `projects/game-nova/backend/internal/auth/handler.go`.

**Заметка**: после фикса D подтверждено end-to-end что для входа в игру
под identity-юзером **не нужен portal handoff flow**. Прямой login через
`http://localhost:5173` (LoginScreen → identity `/auth/login` через
vite-proxy) → `EnsureUserMiddleware` lazy-провижионит юзера в game-nova
→ `Starter.Assign()` создаёт стартовую планету (Homeworld 6:258:11).
Полный flow:
- ✅ `POST /auth/login` → JWT
- ✅ `GET /api/me` → 200, `roles:["admin"]`, `credit:5`
- ✅ `GET /api/planets` → 1 планета, ресурсы тикают

Portal handoff (`POST /auth/universe-token` + `POST /auth/token/exchange`
+ `/auth/handoff` page в game-frontend) реализован в коде, но не
подключен к кнопке «Играть» в `HomePage.tsx` portal'а — она ведёт
прямо на `https://uni01.oxsar-nova.ru` без `?code=`. В dev это
эквивалентно ручному логину на game-frontend, в проде нужен handoff.
**Вынесено в отдельную задачу**, в этом плане не сделано.

### 5. Admin-консоль не запускалась — три рассогласования

Подняли admin-стек поверх dev:
- `docker-compose.admin.yml` дефолт `IDENTITY_URL=http://identity-service:9001`
  — опечатка, identity нигде не слушал 9001.
- `volumes: ./admin-ips.conf:/etc/nginx/admin-ips.conf:ro` — файла
  нет, только `.example`. Без него монтирование падает.
- `BFF_COOKIE_SECURE` дефолт `true` — на dev по `http://localhost`
  cookie с `Secure` не доедет, юзер не залогинится. Default правильный
  для прода, но при локальном запуске нужно `false` (через env).

**Решение**:
- `docker-compose.admin.yml`: дефолт `9001 → 9000` (опечатка) +
  комментарий в шапке.
- `deploy/admin-ips.conf`: создан файл `0.0.0.0/0 1;` (dev — открыто).
  Файл уже в `.gitignore`, в репо лежит только `.example`.
- `BFF_COOKIE_SECURE=false` передаётся через env при dev-запуске
  (default в compose остался `true` для прода).

**Также**: при первом запуске `ADMIN_FRONTEND_PORT` дефолт был `8081`,
который занят dev-backend. Заранее (в начале сессии при аудите портов)
заменили на `8086`.

## Что сделано параллельно

### Тестовые dev-юзеры

Зарегистрированы 6 юзеров в identity-db через `POST /auth/register`,
роли раздал SQL'ом в `user_roles` (механизма bootstrap-superadmin в коде
нет — план 53 §Bootstrap требует ручного SQL). Все с паролем `DevPass123`.

| Логин | Роль | Назначение |
|-------|------|------------|
| `player1` | player | обычный игрок (default) |
| `support1` | support | чтение тикетов, аудита, профилей |
| `mod1` | moderator | модерация UGC, баны |
| `admin1` | admin | игровая админка, planet ops |
| `billing1` | billing_admin | биллинг, возвраты, audit |
| `super1` | superadmin | управление ролями + всё остальное |

Документация: `docs/ops/dev-test-users.md` (новый файл) +
`docs/ops/admin-access.md` (расширены секции «Bootstrap», «Локальный
dev-доступ», «Закладка в браузере»).

### Унификация dev-пароля → `DevPass123`

В сессии было два разных dev-пароля: `DevPass123!` (в наших 6 identity-
юзерах) и `test-password-123` (в testseed game-nova юзерах admin/alice/
bob/eve/charlie). Унифицировал на `DevPass123`:
- testseed: пересоздан с новым паролем (5 game-nova юзеров).
- Identity 6 юзеров: пересозданы через DELETE + register с новым паролем.
- Документация и e2e-фикстуры обновлены.

**Файлы**:
- `projects/game-nova/backend/cmd/tools/testseed/main.go` — `testPassword` константа.
- `projects/game-nova/frontends/nova/e2e/fixtures/auth.ts` — `TEST_PASSWORD`.
- `projects/game-nova/frontends/nova/e2e/README.md` — упоминание.
- `Makefile` — комментарий + echo для `make ui-preview`.
- `deploy/docker-compose.e2e.ports.yml` — комментарий.
- `docs/ops/runbooks/docker-stacks.md` — две строки.
- `docs/ops/admin-access.md`, `docs/ops/dev-test-users.md` — везде.

**Не тронуто**: `docs/project-creation.txt` — это исторический дневник,
запись «Пароль: test-password-123» отражает состояние на момент той
итерации, ретроактивно менять не следует.

## Аудит и заметки, не превратившиеся в правки

### Уникальность host-портов

Аудит compose-файлов (вариант **C** — править только реальные конфликты
для одновременно сосуществующих стеков). Реальных коллизий внутри dev
не было. Единственная правка — `ADMIN_FRONTEND_PORT` default `8081 → 8086`
в `docker-compose.admin.yml`. Prod/e2e/multiverse/monitoring оставлены
как есть — они дизайном переиспользуют порты dev и не запускаются вместе
с dev.

### URL-аудит

По запросу пользователя сделан review SPA-роутов и API-endpoints портала.
Реальная неконсистентность одна — `DELETE /auth/users/me` против
`GET /auth/me` (асимметрия), но фикс оставлен на отдельную задачу.

### Терминология auth ↔ identity

Подтверждено: на бэке сервис = `identity-service`, переменные =
`IDENTITY_*`. На фронте store = `src/store/auth.ts`, эндпоинты на
identity = `/auth/*` (классическая фронт-конвенция: identity = бэк-
сервис, auth = клиентский поток токенов). Переименовывать не нужно.

### Ссылка на админку с публичного портала

Согласовано: **ничего не добавлять**. Архитектура мультидоменная
(`oxsar-nova.ru` ↔ `admin.oxsar-nova.ru` за IP-allowlist), портал не
должен «знать» про админку. Вместо ссылки — секция «Закладка в браузере»
в `docs/ops/admin-access.md` с готовыми Name/URL для prod и dev.

## Lessons learned (зафиксировано в memory)

- На Windows + Docker Desktop inotify-события не передаются из хоста в
  vite-контейнер через volume mount → vite держит закэшированный bundle
  даже при свежих файлах на диске. Симптом: фикс не подхватывается.
  Лечится `docker compose restart <fe>` + hard-reload.
- После любых правок файла, который активно обслуживается docker-
  контейнером, **верифицировать применение** через
  `docker exec <c> grep <маркер> /app/<path>` и (для dev-серверов)
  `curl http://localhost:<port>/src/<file>` — иначе можно отрапортовать
  «починил», а юзер увидит старое поведение и потеряет час.

## Изменённые файлы (полный список)

Compose / инфра (5):
- `deploy/docker-compose.yml` — JWKS у воркеров.
- `deploy/docker-compose.admin.yml` — порт + identity URL.
- `deploy/admin-ips.conf` — новый, gitignored.
- `deploy/docker-compose.e2e.ports.yml` — комментарий.
- `Makefile` — echo dev-пароля.

Backend (1):
- `projects/game-nova/backend/internal/auth/handler.go` — Me handler.
- `projects/game-nova/backend/cmd/tools/testseed/main.go` — пароль.

Frontends (5):
- `projects/portal/frontend/vite.config.ts` — VITE_IDENTITY_TARGET.
- `projects/portal/frontend/src/store/auth.ts` — hydrate().
- `projects/portal/frontend/src/App.tsx` — useEffect(hydrate).
- `projects/game-nova/frontends/nova/vite.config.ts` — VITE_IDENTITY_TARGET.
- `projects/game-nova/frontends/origin/vite.config.ts` — VITE_IDENTITY_TARGET.
- `projects/game-nova/frontends/origin/.env.example` — комментарий.
- `projects/game-nova/frontends/nova/e2e/fixtures/auth.ts` — пароль.
- `projects/game-nova/frontends/nova/e2e/README.md` — пароль.

Документация (3):
- `docs/ops/dev-test-users.md` — новый.
- `docs/ops/admin-access.md` — bootstrap + dev-доступ + закладки.
- `docs/ops/runbooks/docker-stacks.md` — пароль.

## Часть 2 (вечер 2026-04-29): multiverse + переименования

Стек уже стабилен; продолжение сессии — расширение мультивселенной и
наведение порядка в именах сервисов compose.

### 6. Третья dev-вселенная uni03 «Classic» (legacy-PHP balance)

Поднята полная uni03-секция параллельно uni01/uni02:
- `configs/balance/uni03.yaml` — копия `origin.yaml` с `universe: uni03`
  (override балансных коэффициентов: buildings/research/ships/defense/
  rapidfire/requirements; глобалы наследуются от nova). Источник —
  легаси `na_construction`, `na_ship_datasheet`, `na_rapidfire` через
  `cmd/tools/import-legacy-balance` (план 64 Ф.2).
- `configs/universes.yaml` — новая запись uni03 (Classic, ×0.75 как
  Nova, active, dev_url `http://localhost:5176`, num_galaxies 8,
  num_systems 600, protection 24h).
- `deploy/docker-compose.yml` — пять сервисов uni03-{db, migrate,
  backend, worker, frontend} + два named volume (uni03-db-data,
  uni03-frontend-node-modules). DB host-port 5438, backend 8083,
  frontend 5176, redis logical-DB 6.
- portal-backend пересобран — `GET /api/universes` отдаёт три
  вселенные. Карточка «Classic» появилась на http://localhost:5174.

Loader умел работать с произвольным `universe_id` уже в
`internal/balance/loader.go:164` (`LoadFor`) — единственное условие
было «существует ли `configs/balance/<id>.yaml`». Никаких правок Go-
кода не понадобилось. Backend на старте логирует: `balance bundle
loaded universe_id=uni03 override_applied=true buildings=22 ships=25`.

End-to-end проверено:
- POST `/auth/login` (admin1/DevPass123) через uni03-frontend → 200 + JWT.
- GET `/api/me` через vite-proxy → 200, JWT roles из identity.
- `EnsureUserMiddleware` создал `users` запись в **uni03-db** (не в
  uni01-db / uni02-db), `Starter.Assign()` создал стартовую планету
  Homeworld 6:258:11 с применённым origin-балансом.

**Изменённые файлы (часть 6)**:
- `projects/game-nova/configs/balance/uni03.yaml` (новый, gitignored
  list не задеваем).
- `projects/game-nova/configs/universes.yaml`.
- `deploy/docker-compose.yml`.

### 7. uni03-frontend = origin-фронт

Изначально uni03-frontend был nova-фронтом. Логичнее было использовать
origin-фронт (legacy pixel-perfect клон): «legacy баланс на бэке +
legacy UI на фронте» — внутренне согласовано.

Origin-фронт раньше в docker не запускался (своего Dockerfile нет),
но `nova/Dockerfile` достаточно generic — bind-mount перекрывает
исходники. Ловушка: Docker-image при build кэширует `npm install` под
nova-package.json, а origin требует другие зависимости (например,
`react-router-dom`). Bind-mount + named volume для node_modules
оставляли node_modules **пустыми** — vite падал с «react-router-dom not
found». Лечится переопределением `command: sh -c "npm install &&
npm run dev"` для uni03-frontend (npm install прогоняется в runtime,
node_modules заполняется правильно для origin'а).

Особенности:
- Vite origin'а слушает 5174 (см. `vite.config.ts` origin'а), поэтому
  port mapping `5176:5174` (а не `5176:5173`).
- Добавлено `VITE_PORTAL_BASE_URL=http://portal-backend:8090` для
  S-045 Support (план 56) — origin делает прямой fetch на portal
  с Bearer-токеном (не через vite-proxy).

**Изменённые файлы (часть 7)**:
- `deploy/docker-compose.yml` — uni03-frontend volume + command.

### 8. Переименование `frontend` → `uni01-frontend`

После добавления uni02/uni03 голое имя `frontend` визуально
несимметрично с `uni02-frontend` / `uni03-frontend`. Переименовали
для консистентности:
- сервис в compose, volume `frontend-node-modules` → `uni01-frontend-
  node-modules`.
- override в `prod.yml`, `e2e.yml`, `e2e.ports.yml`.
- nginx upstream в `nginx.frontend.conf` (но это конфиг прод-образа,
  где `proxy_pass` теперь смотрит на `uni01-backend` — см. часть I).
- два упоминания в `docs/plans/78` и `docs/prompts/plan-78`.

**Изменённые файлы (часть 8)**:
- `deploy/docker-compose.yml`.
- `deploy/docker-compose.prod.yml`.
- `deploy/docker-compose.e2e.yml`, `deploy/docker-compose.e2e.ports.yml`.
- `docs/plans/78-frontends-layout-refactor.md`,
  `docs/prompts/plan-78-frontends-layout-refactor.md`.

### 9. Переименование `backend` → `uni01-backend`

Симметрично с frontend. Все ссылки на хост `backend:8080` в compose-
файлах, nginx.frontend.conf и активных runbook'ах переписаны на
`uni01-backend:8080`. Особенность: admin-bff проксирует только в
**uni01** игровой сервер (`GAME_NOVA_URL` дефолт). Это by design —
админ работает с uni01; для uni02/uni03 нужен env override (out of
scope этого плана).

**Изменённые файлы (часть 9)**:
- `deploy/docker-compose.yml`, `deploy/docker-compose.e2e.yml`,
  `deploy/docker-compose.e2e.ports.yml`.
- `deploy/docker-compose.admin.yml` — `GAME_NOVA_URL` дефолт.
- `deploy/docker-compose.monitoring.yml` — комментарий.
- `deploy/nginx.frontend.conf` — два `proxy_pass`.
- `docs/ops/runbooks/docker-stacks.md` — две упоминания.
- `docs/ops/runbooks/backup-and-monitoring.md` — пример nginx.
- `docs/ops/release-process.md` — yaml-сниппет prod-overlay.

Не тронуты:
- `multiverse.yml` — там собственная схема `uni01-backend` /
  `uni02-backend` через YAML-anchor `x-backend-common`.
- `docs/plans/{36, 54, 78, 79}` — исторические упоминания «frontend» /
  «backend» в архивных планах, ретроактивно не правим.

### 10. Воркеры игнорировали `UNIVERSE_ID` + переименование `worker` → `uni01-worker`

При проверке логов uni02-worker и uni03-worker обнаружилось, что **все
три воркера** грузили `balance bundle universe_id=uni01,
override_applied=false`. Причина — в env воркеров не задана
`UNIVERSE_ID`, общий config-loader падал на default `uni01`. Это
значит uni02-worker и uni03-worker писали в свои БД (uni02-db, uni03-
db — это правильно по `DB_URL`), но тикали события по uni01-балансу
(не по uni02 ускорению ×4 и не по uni03 origin-override).

Семантически — это близкий родственник бага из части 1 (`IDENTITY_JWKS_URL`
у воркеров): backend получил env, воркер — нет. Аналогичный недосмотр
при copy-paste compose-секций.

**Фикс**: добавил `UNIVERSE_ID: uni01` / `uni02` / `uni03` в env
соответствующих воркеров. После пересоздания:
- uni01-worker → `universe_id=uni01, override_applied=false` (для
  uni01 нет override-файла, как и было задумано).
- uni02-worker → `universe_id=uni02, override_applied=false` (override
  отсутствует — `configs/balance/uni02.yaml` нет; loader корректно
  возвращает дефолт).
- uni03-worker → `universe_id=uni03, override_applied=true` (legacy-
  PHP balance подцеплен) ✅.

**Обсуждалось не включать остальное** (`GAMESPEED`, `NUM_GALAXIES`,
`NUM_SYSTEMS`, `PROTECTION_PERIOD`): пользователь предпочитает,
чтобы эти параметры backend и воркер брали из `configs/universes.yaml`
по `UNIVERSE_ID`, а не из env. Сейчас они так не работают — это
отдельная задача (out of scope).

**Заодно — переименование `worker` → `uni01-worker`** для симметрии
с `uni02-worker` / `uni03-worker`. Затронутые файлы:
- `deploy/docker-compose.yml` — определение сервиса.
- `deploy/docker-compose.scaling.yml` — service + комментарии (replicas
  для масштабирования воркера на одном VPS).
- `deploy/docker-compose.e2e.yml` — service.
- `deploy/docker-compose.monitoring.yml` — комментарий с metrics URL.
- `deploy/prometheus.yml` — job `worker → uni01-worker`, targets
  `deploy-worker-{1,2} → deploy-uni01-worker-{1,2}`. Заодно исправлен
  забытый от прошлого rename `targets: ["backend:8080"]` →
  `["uni01-backend:8080"]` (job `backend → uni01-backend`).

**Изменённые файлы (часть 10)**:
- `deploy/docker-compose.yml` (env воркеров + rename).
- `deploy/docker-compose.scaling.yml`.
- `deploy/docker-compose.e2e.yml`.
- `deploy/docker-compose.monitoring.yml`.
- `deploy/prometheus.yml`.

### 11. Аудит универсум-параметров: какие живые, какие мёртвые

При обсуждении миграции конфигов на `universes.yaml` понадобилось
разобраться, какие из 8 per-universe параметров (`GAMESPEED`,
`NUM_GALAXIES`, `NUM_SYSTEMS`, `MAX_PLANETS`, `BASHING_PERIOD`,
`BASHING_MAX_ATTACKS`, `PROTECTION_PERIOD`, `DEATHMATCH`) реально
используются в runtime, а какие — мёртвый код в `Config.Game`.

**Живые (5)** — читаются за пределами config:
- `GAMESPEED` — `economy/formulas.go:37`, `fleet/transport.go:298` и в
  building/research/rocket/repair сервисах (формула: длительность / speed).
- `MAX_PLANETS` — `fleet/colonize.go:120-121` (override
  computer_tech-bonus).
- `BASHING_PERIOD`, `BASHING_MAX_ATTACKS` —
  `fleet/transport.go:859-903` (anti-bashing limit).
- `PROTECTION_PERIOD` — `fleet/attack.go:152` (SQL-фильтр защиты
  новичков).

**Мёртвые (3)** — определены в `Config.Game`, парсятся из env, но в
runtime никем не читаются:
- `NUM_GALAXIES`, `NUM_SYSTEMS` — в game-nova `internal/galaxy/`,
  `internal/planet/starter.go`, `internal/fleet/expedition.go`
  координаты валидируются жёстко закодированными `1..16 x 1..999`;
  starter генерирует `r.IntN(8)+1 / r.IntN(500)+1`. То есть на
  uni02 `NUM_GALAXIES=4 / NUM_SYSTEMS=300` из env эффекта не имеют.
- `DEATHMATCH` — `Config.Game.Deathmatch` парсится, но не читается
  ни одним обработчиком.

Аудит legacy-PHP `projects/game-legacy-php/` показал, что для NG/NS
**функционал есть и обширный** (валидация координат при атаках/
телепортах/экспедициях, кольцевая топология систем `min(|s1-s2|,
NUM_SYSTEMS-|s1-s2|)`, генерация стартовых планет в правильном
диапазоне) — это **недостающий функционал в Go**, а не «было удалено».
Поэтому вместо «удалить мёртвые поля» решили **достать функционал
из legacy** в часть 12.

DEATHMATCH в legacy — набор **11 переключателей** для отключения фич
в DM-режиме (alien_enabled, expedition_enabled, newbie_protection,
max_building_level=35 vs 40, profession_change_min_days=7 vs 14,
mission_halting, exch_inviolable, show_user_agreement, show_dm_points,
admins_list, и не интересующие ACHIEVEMENTS/TUTORIAL). Эту функцию
**не реализуем в плане 72.1** (≈24+ часов на 11 точек интеграции
с тестами). Записано как TODO в `docs/simplifications.md`
секция Infrastructure → `[config] DEATHMATCH-режим` для будущей
реализации с детальным аудитом каждого пункта.

### 12. ✅ Миграция параметров вселенной на `universes.yaml` (single source of truth)

**Реализовано** в коммитах `166b0e5` … `d6ea7b7` (29 апреля 2026).

Сейчас 15 per-universe параметров в `Config.Game.*` парсятся из env
compose. У `uni01-backend` env пуст — fallback на code-defaults. У
`uni02-backend` / `uni03-backend` в env заданы 4 переменные
(`GAMESPEED`, `NUM_GALAXIES`, `NUM_SYSTEMS`, `PROTECTION_PERIOD`),
которые **дублируются** в `configs/universes.yaml`. Никто не
синхронизирует между ними — побеждает env, YAML же читается только
portal-backend для отображения карточек на портале. Это
рассинхрон-риск.

**Цель**: `configs/universes.yaml` становится единственным
источником истины. Backend читает все параметры через
`universe.Registry.ByID(univID)` после парсинга `UNIVERSE_ID`.
env compose очищается от дублей.

**Дизайн-решения** (согласовано с пользователем):

1. **Все 15 параметров — обязательные** в YAML. При `Load()`
   валидация: если хоть одно поле отсутствует в любой вселенной —
   fail-fast с понятной ошибкой.
2. **Указатели для парсинга** (`*int`, `*float64`, `*bool`) —
   различает «поле отсутствует» от «явно `0` / `false`». После
   валидации конвертируется в обычные типы. Иначе невозможно
   отличить `MaxPlanets: 0` (валидное «computer_tech+1») от
   «забыли указать поле».
3. **Fallback на uni01** для **6 новых полей** (StorageFactor,
   ResearchSpeedFactor, EnergyProductionFactor, TeleportCostOxsars,
   TeleportCooldownHours, TeleportDurationMinutes) — указываются
   только в `uni01`, остальные вселенные при отсутствии получают
   значения из uni01. Если в будущем нужно особое значение —
   пишут явно.
4. **`Universe` struct в game-nova и portal — синхронизировать**
   (явный «DUPLICATE» comment). После Load — fallback-merge с
   uni01 для отсутствующих 6 полей.
5. **portal `/api/universes` JSON** — отдаёт только текущий
   публичный набор полей; новые 6 (StorageFactor, etc.) на
   карточках не показываются — это детали баланса для админов,
   не для игроков.

**15 параметров** (Speed, Universe-name, Deathmatch, NumGalaxies,
NumSystems, StorageFactor, ResearchSpeedFactor,
EnergyProductionFactor, MaxPlanets, BashingPeriod, BashingMaxAttacks,
ProtectionPeriod, TeleportCostOxsars, TeleportCooldownHours,
TeleportDurationMinutes).

**Расширение схемы YAML** — 6 новых полей **только в uni01** со
значениями = текущие code-defaults:

```yaml
- id: uni01
  ...
  storage_factor: 5.0
  research_speed_factor: 2.0
  energy_production_factor: 0.8
  teleport_cost_oxsars: 50000
  teleport_cooldown_hours: 24
  teleport_duration_minutes: 0
```

**Реализация NG/NS-функционала из legacy-PHP** (часть, ради которой
изначально начали миграцию):

1. **Валидация координат** — `internal/galaxy/service.go`
   `Coords.Validate()` принимает `numGalaxies`, `numSystems`
   параметрами. Текущие hardcoded `1..16 x 1..999` уходят.
2. **Кольцевая топология систем** — `Distance()` для системной
   разницы:
   ```go
   diffSys := absInt(a.System - b.System)
   if numSystems > 0 && diffSys > numSystems/2 {
       diffSys = numSystems - diffSys
   }
   ```
3. **Генерация стартовой планеты** — `internal/planet/starter.go:93-94`
   и `internal/fleet/expedition.go:509-510` заменяют hardcoded
   `r.IntN(8)+1 / r.IntN(500)+1` на параметры из cfg.
4. **Лимиты телепорта** —
   `internal/planet/teleport_handler.go:70-77` убрать hardcoded
   `coordGalaxyMax=16, coordSystemMax=999` на параметризацию.

**Затронутые файлы**:
- `projects/game-nova/configs/universes.yaml` (+6 полей в uni01).
- `projects/game-nova/backend/internal/universe/registry.go`
  (расширить Universe struct + валидация Load).
- `projects/portal/backend/internal/universe/registry.go` (DUPLICATE).
- `projects/game-nova/backend/internal/config/config.go` —
  переписать `Game:` секцию на чтение через `registry.ByID(univID)`,
  удалить env-парсинг 15 переменных. UniverseID получает env как
  раньше.
- `cmd/server/main.go`, `cmd/worker/main.go` — после `cfg.Load()`
  получить `Registry` (если ещё не получен) и заполнить
  `cfg.Game.*` из `registry.ByID(cfg.Auth.UniverseID)`.
- `deploy/docker-compose.yml` — удалить env у uni02-backend и
  uni03-backend (`GAMESPEED`, `NUM_GALAXIES`, `NUM_SYSTEMS`,
  `PROTECTION_PERIOD`).
- `internal/galaxy/service.go` — `Coords.Validate(numG, numS)`,
  `Distance(a, b, numS)` кольцевая.
- `internal/galaxy/service_test.go` — адаптировать.
- `internal/planet/starter.go` — параметризация.
- `internal/planet/teleport_handler.go` — параметризация.
- `internal/fleet/expedition.go` — параметризация.
- `internal/fleet/transport_test.go` — пересмотреть, сигнатуры
  Distance изменились.

**Что НЕ делается в этой сессии** (отложено):
- Поиск планет в ±N систем для экспедиций (legacy
  `Expedition.class.php:1007-1008`) — кольцевая логика поиска
  соседних систем. В Go экспедиции уже работают с рандомным
  поиском координат, не «соседние системы». Сейчас исправляем
  только hardcoded числа.
- DEATHMATCH-функционал (см. часть 11 — отдельная задача).
- `internal/fleet/moon_destruction.go`,
  `internal/origin/alien/handlers.go` — Explore-агент пометил
  «не просмотрены»; если там есть дополнительная валидация
  координат — найдём по ходу или отдельной итерацией.

**End-to-end проверка** (после реализации, на трёх вселенных uni01/
uni02/uni03):
- `balance bundle loaded universe_id=uni01, override_applied=false`.
- `balance bundle loaded universe_id=uni02, override_applied=false`
  (нет файла `balance/uni02.yaml`, fallback на дефолт корректный).
- `balance bundle loaded universe_id=uni03, override_applied=true`
  (legacy-PHP override применён).
- `GET /api/galaxy/9/100` → HTTP 400 (за `numGalaxies=8`) ✅.
- `GET /api/galaxy/8/600` → HTTP 200 (граница, валидно) ✅.
- На uni03 (origin-фронт + legacy-баланс) валидация работает идентично.

**Получившиеся коммиты части 12**:
- `166b0e5` feat(universe) — Universe struct + YAML schema +6 полей,
  парсинг указателями, fallback на uni01.
- `9c79d22` refactor(config) — Load() без env, добавлен ApplyUniverse.
- `b22dce5` feat(cmd) — server/worker wire-up universes.yaml→cfg.Game.
- `348a8bd` feat(galaxy) — Validate(numG,numS) + кольцевая Distance.
- `d849969` fix(planet,fleet,rocket) — параметры в Starter, TeleportConfig,
  TransportService, expExtraPlanet, rocket.Service.
- `d6ea7b7` chore(deploy) — удаление env-дублей в uni02-backend и
  uni03-backend.

### 13. ✅ portal: кнопка «Играть» в карточке uni03 вела на prod-URL

После реализации части 6 (uni03 «Classic») в карточке вселенной на
портале http://localhost:5174 кнопка «Играть» давала ссылку
`https://uni03.localhost` вместо `http://localhost:5176`. Симптом
визуально виден в строке статуса браузера при наведении на кнопку.

**Причина**: `projects/portal/frontend/src/pages/HomePage.tsx:12-19`
формирует URL через VITE-переменные:
- если есть `VITE_UNIVERSE_PORT_<subdomain>` → `http://localhost:<port>`,
- иначе fallback `https://<subdomain>.<VITE_BASE_DOMAIN>`.

В env portal-frontend в compose были только `VITE_UNIVERSE_PORT_uni01`
и `VITE_UNIVERSE_PORT_uni02`. Для uni03 переменная отсутствовала —
сработал prod-fallback.

**Фикс** (`d.deploy/docker-compose.yml`): добавил
`VITE_UNIVERSE_PORT_uni03: "5176"` рядом с uni01/uni02.

**Изменённые файлы (часть 13)**:
- `deploy/docker-compose.yml`.

**Заметка для будущего**: при добавлении uni04+ нужно одновременно
обновлять три места — `configs/universes.yaml`, compose-секции
uni0N-{db,migrate,backend,worker,frontend} и env portal-frontend
`VITE_UNIVERSE_PORT_uni0N`. Кандидат на автоматизацию (генерация
из `universes.yaml`) — отложено в out-of-scope.

### 14. ✅ i18n не загружался — 3-уровневые YAML + битые `t()`-вызовы

После handoff'а в game-фронте (uni02/uni03) везде показывались
плейсхолдеры `[global.menuMain]`, `[overview.planetDiameter]`. На
uni01 переводы случайно работали — браузер кешировал словарь в
localStorage с прошлого захода (см. `i18n.tsx`
`LOCALE_STORAGE_PREFIX`). На свежих доменах (uni02:5175,
uni03:5176) localStorage пуст → запрос на `/api/i18n/ru` →
**404** (handler возвращает 404 если bundle nil).

**Корневая причина**: backend `internal/i18n/i18n.go:32` объявляет
`type Dict map[string]map[string]string` — двухуровневый. YAML-
файлы `configs/i18n/{ru,en}.yml` имели **3-уровневые блоки**:

```yaml
exchange:                   # group
  validation:               # подгруппа (3-й уровень)
    pickArtefact: "..."
  errors:                   # подгруппа (3-й уровень)
    insufficientArtefacts: "..."

event:                      # group
  exchange:                 # подгруппа
    expire:                 # 4-й уровень
      subject: "..."
    ban:
      subject: "..."
```

`yaml.Unmarshal` при попытке прочитать map[string]string из map (3-й
уровень) падал с `cannot unmarshal !!map into string`. На уровне
i18n.Load это превращалось в WARN «i18n not loaded» → bundle = nil
→ handler 404. **Все три бэкенда** (uni01/02/03) на самом деле НЕ
загружали словарь, но фронт uni01 показывал переводы из браузерного
кэша — это маскировало баг.

**Фикс** (плоская YAML-структура — пользователь выбрал вариант A
«вынести на корень»):

1. `event.exchange.{expire,ban}.subject` — **удалён целиком** в обоих
   файлах. Был мёртвый код: ни backend (`Tr`), ни frontend (`t()`)
   на эти ключи не ссылались. Подтверждено grep'ом.
2. `exchange.validation.*` (6 ключей) → корневая группа `validation:`
   с теми же ключами.
3. `exchange.errors.*` (11 ключей) → корневая группа `errors:` с
   теми же ключами.

Имена `validation` и `errors` слишком общие, но согласованы с
пользователем — это короткий путь к устранению блокера. В будущем
можно специализировать (например `exchangeValidation`,
`exchangeErrors`) если появятся другие места с тем же именем.

**Сопутствующий фикс** во фронте — `CreateLotPage.tsx`:

Существующие вызовы были однопараметрические со склееным ключом —
**не работали** даже до моей правки YAML:

```ts
// Было (broken):
t(`errors.${code}` as never)
t(`validation.${validationKey}` as never)
```

`t()` принимает либо `(group, key)` либо `(key)` с `defaultGroup`
(см. `i18n/i18n.tsx:127-153`). Однопараметрический вариант с
точкой искал ключ `errors.<X>` в группе `exchange` (`useTranslation('exchange')`)
— не находил, возвращал `[exchange.errors.<X>]`.

```ts
// Стало:
t('errors', code)
t('validation', validationKey)
```

`as never` удалён — больше не нужен (TypeScript-сигнатура корректна).

**End-to-end проверка после rebuild backends**:

```
$ docker logs deploy-uni01-backend-1 | grep i18n
{"level":"INFO","msg":"i18n loaded","langs":["en","ru"]}      ✅

$ for p in 8081 8082 8083; do curl -so/dev/null -w"%{http_code} %{size_download}\n" http://localhost:$p/api/i18n/ru; done
200 274305
200 274305
200 274305      ✅
```

Парсинг YAML вне docker (тест из плана):
```
ru.yml: OK groups=68 (validation=6, errors=11, lots=0)
en.yml: OK groups=68 (validation=6, errors=11, lots=0)
```

`lots=0` — корневой группы `lots` никогда и не было; ключи
`submitCreate`, `cancelForm`, etc были под `exchange:` (я перепутал
читая dump'ы grep'а в первый раз).

**Изменённые файлы (часть 14)**:
- `projects/game-nova/configs/i18n/ru.yml` — вынос validation/errors
  на корень + удаление event-блока.
- `projects/game-nova/configs/i18n/en.yml` — то же.
- `projects/game-nova/frontends/nova/src/features/exchange/CreateLotPage.tsx`
  — 2 двухаргументных `t()`-вызова.

**Заметка про uni03 origin-фронт**: в нём показ плейсхолдеров типа
`[global.menuMain]` указывает на отдельный i18n-набор (origin —
другой бандл, не nova). Этот фикс касается только nova-фронта (uni01,
uni02) и backend i18n. Origin-i18n — отдельная задача (out-of-scope).

### 15. ✅ Bootstrap race — стартовая планета не успевала создаться

**Симптом**: при первом входе нового юзера в game-вселенную (через
handoff) origin-фронт показывал «Нет планет. Попробуйте перезагрузить
страницу.», а в логах backend'а появлялись пары предупреждений:

```
"retrying starter planet assign" user_id=019dd849-...
"retry starter planet assign failed" err="duplicate key value violates
unique constraint planets_galaxy_system_position_is_moon_key"
```

**Корневая причина**: race-condition в `EnsureUserMiddleware`:

1. Origin-фронт сразу при загрузке делает 4-5 параллельных запросов
   (`/api/me`, `/api/planets`, `/api/messages/unread-count`,
   `/api/fleet/incoming` и т.д.).
2. Все попадают в middleware. Первый INSERT проходит, `RowsAffected==1`
   → `go bootstrapNewUser(...)` (асинхронно).
3. Параллельные запросы видят `RowsAffected==0` (ON CONFLICT DO
   NOTHING) → `go retryBootstrapIfNeeded(...)`. Этот retry проверяет
   `cur_planet_id IS NULL` — но первый bootstrap ещё не дошёл до
   `Starter.Assign`, флаг ещё не установлен → условие true →
   параллельно вызывается **второй** `Starter.Assign`.
4. Оба `Starter.Assign` пытаются вставить планету в БД на одни и те
   же координаты → один проходит, второй падает с UNIQUE-violation.
5. Параллельно `/api/planets` возвращает **пустой массив** (юзер уже
   есть, но планеты ещё нет) → фронт показывает «Нет планет».

**Фикс**: `Starter.Assign` теперь делается **синхронно** при
`RowsAffected == 1` в `EnsureUserMiddleware` — до возврата управления
handler'у. Это гарантирует что:
- К моменту когда **любой** handler получит запрос (включая
  `/api/planets`) — планета уже создана.
- Параллельные запросы получают `RowsAffected==0` и идут через
  `retryBootstrapIfNeeded`, который видит `cur_planet_id != NULL`
  → no-op. Никаких race-call'ов на `Starter.Assign`.

Welcome-сообщение и `universe_membership` в identity остаются
**асинхронными** (выделены в отдельную функцию `bootstrapAsyncTail`)
— они не блокируют первый рендер UI. План 36 Critical-5 (membership
в identity) не страдает: до следующей выдачи JWT membership
зарегистрируется, а первый JWT уже выдан с пустым `active_universes`
(handoff не зависит от этого поля).

**Trade-off**: первый запрос юзера медленнее на ~50ms (синхронный
INSERT планеты + начальные ресурсы + здания + флот). Это приемлемо
— одноразовый цена при регистрации в каждой вселенной.

**End-to-end проверка** после rebuild backends:

```
$ docker exec deploy-uni03-db-1 psql ... DELETE FROM users WHERE username='player1';
DELETE 1

$ # handoff player1 → uni03 (новый JWT)
$ curl POST /api/universes/uni03/session ... → redirect_url + code
$ curl POST /auth/token/exchange ... → fresh access+refresh

$ # Первый запрос /api/me → 200 + lazy-create + sync Starter.Assign
$ curl GET http://localhost:8083/api/me → 200 OK

$ # Сразу же /api/planets — планета уже есть
$ curl GET http://localhost:8083/api/planets
{"planets":[{"name":"Homeworld","galaxy":5,"system":416,"position":5,...}]}

$ docker logs uni03-backend | grep -i "retry\|duplicate"
# (пусто — race устранён)
```

**Изменённые файлы (часть 15)**:
- `projects/game-nova/backend/internal/auth/ensure_user.go`:
  - В `EnsureUserMiddleware` при `RowsAffected==1` синхронный
    `Starter.Assign` до `next.ServeHTTP`.
  - Функция `bootstrapNewUser` переименована в `bootstrapAsyncTail` —
    теперь только welcome-message + universe_membership.

### 16. ✅ Origin MainScreen — pixel-perfect доводка под legacy main.tpl

**Симптом**: на http://localhost:5176/ origin показывает почти пустую
таблицу (имя планеты + 3 ресурса), сравнение с эталоном
http://localhost:8092 (legacy-php) выявило что origin/MainScreen
реализует ~10% от legacy main.tpl. Параллельно TopHeader работает как
placeholder (выводит UUID юзера и `—` вместо ресурсов).

**Эталон** — `projects/game-legacy-php/src/templates/standard/main.tpl`.

#### Требования и ограничения (согласованы с пользователем)

1. **Pixel-perfect зеркало legacy-PHP**. Origin портируется от
   `projects/game-legacy-php`. Все различия с эталоном — баги, не
   фичи; все элементы legacy должны быть воспроизведены если есть
   данные в API. Дизайн-отклонения недопустимы.
2. **Правки фронтенда — только в `projects/game-nova/frontends/origin/`**.
   - **Origin** (`projects/game-nova/frontends/origin/`) — целевой
     фронт, который мы дотягиваем до pixel-perfect-копии legacy-PHP.
     Все UI-правки идут сюда.
   - **Nova** (`projects/game-nova/frontends/nova/`) — параллельный
     фронт другой стилистики (uni01/uni02), **НЕ трогаем** в серии
     pixel-perfect (части 16, 17, 18). Если что-то нужно из nova —
     только **читать** (для образца / inspection), не редактировать.
   - **Legacy-PHP** (`projects/game-legacy-php/`) — эталон-источник.
     **Не правим логику**, только если нужны fixture-данные
     (snapshot-fix, миграции тестовых записей, dev-login.php). Для
     порта берём оттуда `.tpl`-шаблоны, CSS, картинки — копируем в
     origin как ассеты.
   - **Backend** (`projects/game-nova/backend/`) — правим **точечно
     по требованию п.6**: расширение API когда legacy показывает
     поле, которого у нас нет, и оно реально нужно. Не правим
     game-логику, миграции — только handlers / DTO / типы.
   - **Identity / portal / billing** — НЕ трогаем в серии
     pixel-perfect; они касаются auth/biling, не UI.
3. **Без захардкоженных текстов**. Все строки UI идут через i18n
   (`t('group', 'key')`). Английский неполный, но обязательно
   поддерживается.
4. **Формат i18n-плейсхолдеров — `{{var}}`** (наш стандарт, как у
   `i18next`/Vue I18n). Старый PHP-формат `{@var}` мигрируется на
   `{{var}}` при касании (technical debt). ICU MessageFormat не
   используем — overhead.
5. **Подбор i18n-ключей**: сначала искать существующий ключ в
   подходящей группе (`overview.*`, `main.*`, `global.*`); новый
   ключ только если в существующих нет ничего подходящего. Имя
   группы — семантическое (где UI это показывает).
6. **Расширяем backend если нужно — но грамотно**. Если поле,
   которое legacy показывает (опыт, очки, ранг, шахтёр-level,
   межгал. исследования, и т.п.), отсутствует в `/api/me` или
   `/api/planets` — расширяем API. Перед расширением **проверять
   нужна ли эта функциональность вообще** (возможно её нет в API
   потому что она не нужна в новой игре — план 60 удалил
   реферальную систему, plan 52 удалил `users.role` и т.д.). Если
   нужна — расширяем; если не нужна — фиксируем в out-of-scope с
   обоснованием. Сама часть 16 ограничилась полями которые уже
   были в API.
7. **Удаление дублей с TopHeader**. Старая редакция MainScreen
   дублировала ресурсы в самой таблице (legacy так не делает —
   ресурсы только в шапке topHeader). Дубли удалены в рамках
   pixel-perfect-выравнивания.
8. **Pixel-perfect = и стили, и раскраска, и layout**. Origin
   копируется не только по тексту/полям, но и по **визуальному
   виду**: цветовая схема (синие шапки, контрастные строки),
   шрифты, отступы, размеры столбцов, расположение кнопок,
   декоративные элементы (рамки `<corners>`-стиля), способы
   подсветки активной планеты в sidebar и т.д. Любая правка
   соответствующего origin-компонента сверяется с тем как он
   выглядит в legacy-эталоне на http://localhost:8092 при том же
   юзере (test). Отклонения только если есть явное обоснование
   (например, legacy CSS использует устаревшие техники, или фича
   удалена планом).
9. **Картинки планет/лун — обязательны**. Legacy показывает
   `/images/planets/<planet_type>NN.jpg` и `/mond.jpg` для лун
   (см. main.tpl `{@planetImage}` / `{@moonImage}`). Origin тоже
   должен показывать. Поле `planet_type` уже в API; нужно только
   asset-mapping и настройка раздачи статики. Это часть pixel-
   perfect-задачи.
10. **Современные стандарты для всего нового — особенно API**.
    Если добавляем эндпоинт, поле, миграцию, тип, компонент —
    делаем по принятой в индустрии 2025-практике, не копируем
    legacy «как есть». В частности:

    **REST API**:
    - URL — существительные plural (`/api/users/me`, не
      `/api/getUser`).
    - Versioning — `/api/v1/...` если контракт меняется, иначе
      backward-compat.
    - JSON snake_case (как уже принято в openapi.yaml R1).
    - Идемпотентность (R9): mutating методы принимают
      `Idempotency-Key`.
    - Ошибки — единый envelope `{error: {code, message}}`,
      коды HTTP по семантике (401/403/404/409/429/503).
    - Pagination через `?limit=&offset=` или `?cursor=` —
      не отдавать unbounded списки.
    - Не возвращать "сырые" БД-числа без агрегации (например,
      `points` отдавать вместе с `rank` если оба нужны UI —
      одним запросом, не двумя).

    **Контракты** (DTO):
    - Обязательные/optional поля явно в openapi.yaml + типы
      origin (`schema.d.ts` через `npm run gen:api`).
    - Не пихать UI-конкретику в DTO (например, отформатированные
      строки `36.038.840` — клиент сам форматирует число).
    - Поля legacy (`e_points`/`be_points`) переименовываются в
      smimantic (`combat_experience` / `accumulated_experience`).
      Это согласуется с `currency-rebranding` (ADR-0009): мы
      уже переименовали `credit→oxsarit`, теперь так же с
      экспериментом и т.д.

    **Миграции БД**:
    - Никогда не `DROP COLUMN` в один шаг — сначала перестают
      использовать в коде, потом отдельной миграцией удаляют.
    - `NOT NULL DEFAULT` для новых полей чтобы старые строки
      не падали.

    **Frontend**:
    - TanStack Query для server state, Zustand для UI state.
    - Все запросы через `api.get/post/...` (не raw `fetch`).
    - Без `any`, `@ts-ignore` (только `@ts-expect-error` с
      описанием — но **никогда не из-за лени**, только если
      реально tooling-ограничение).

    **Безопасность**:
    - Нечего показывать одному юзеру про другого без
      явного permission-check'а.
    - Идентичная UI-логика: владелец/чужой/админ — три разных
      view'а одного экрана.

    Если для конкретной фичи нужно отойти от стандартов
    (например legacy-флоу слишком завязан на `{@var}`-
    плейсхолдерах) — обоснование пишем в `docs/simplifications.md`
    с планом возврата.
11. **Prod-quality, без упрощений** (зафиксировано пользователем
    дословно: «делаем без упрощений, как для прода для всех задач
    в этой сессии»). Распространяется на ВСЕ части серии
    pixel-perfect (16, 17, 18) и на параллельные планы 72.2,
    72.3.

    Что это значит конкретно:
    - **Никаких TODO-заглушек**, mock-данных в проде, hardcoded
      значений «на потом». Если что-то не реализовано — не
      коммитим частично, либо доводим до конца, либо явно
      выносим в out-of-scope с тикетом.
    - **Никаких `any`, `@ts-ignore`, `panic()` в hot path,
      `_ = err`** для скрытия ошибок. Все ошибки обрабатываются
      или явно `slog.Error` + return.
    - **Тесты обязательны** для нового бизнес-кода (handler /
      service / repo). Покрытие согласно CLAUDE.md (≥70%
      обычный, ≥85% battle/economy/event).
    - **Миграции БД — обратимые** (up + down), безопасные на
      проде (NOT NULL DEFAULT, никаких локов на больших таблицах
      без `CONCURRENTLY`).
    - **i18n полный** (ru + en), не «en пустой допилим потом».
    - **Логи структурированные** (`slog` с полями `user_id`,
      `planet_id`, `event_id`, `trace_id`).
    - **Permission-checks везде** где есть юзер-данные.
      Никаких «потом добавим RBAC».
    - **OpenAPI обновляется в том же PR** где меняется
      handler/DTO. Не «openapi отдельно потом».
    - **Сгенерированные клиенты** (`schema.d.ts`) пересобираются
      и коммитятся вместе с openapi-правкой.

    Любое отклонение от прод-стандартов = запись в
    `docs/simplifications.md` с обоснованием и планом возврата
    в момент принятия (не «потом»). Без записи отклонения
    недопустимы.

**Скоуп части 16** (ограничен полями уже доступными в API):
- TopHeader: реальные ресурсы (металл/кремний/водород/энергия) +
  кредиты + кнопка «Пополнить» + username + кнопка «Выйти».
- MainScreen строки: заголовок `<currentPlanet> [coords] (<username>)`,
  Главная планета, Профессия, Серверное время, Диаметр, Температура,
  Координаты.

**Что НЕ делается в части 16, переносится в часть 17 «pixel-perfect
add-ons» (НЕ out-of-scope — это продолжение pixel-perfect-задачи)**:

Решения пользователя по скоупу части 17 (зафиксировано 2026-04-29):
- **Очки/ранг/опыт — делаем** (`points` + `rank`, `e_points`/`combat_experience`,
  `be_points`/`accumulated_experience`, шахтёр-level/`offPoints`/`need_points`,
  межгалактические исследования). Расширяем `/api/me` (или отдельный
  `/api/users/me/stats`) согласно требованию п.10 — REST-стандарты,
  semantic field naming, не копируем legacy-имена.

  **ВАЖНО**: в плане 69 Ф.0 категории `dm_points` / `be_points` /
  `of_points` / `of_level` были отвергнуты как YAGNI (см.
  [docs/plans/69-remaster-domain-fields-extension.md §171,181](
  ../plans/69-remaster-domain-fields-extension.md)). Это решение
  **отменено пользователем 2026-04-29** в рамках плана 72.1 ч.17.
  Обоснование отмены: pixel-perfect-задача требует показывать эти
  поля на MainScreen, а decorative-показ без реальной механики
  нарушает принцип «без упрощений» (п.11). Поэтому возрождаем
  **полноценные** механики (с миграциями, бизнес-логикой, тестами):

  - `e_points` уже есть в БД и обновляется в `attack.go:734`
    при бое (проверить корректность legacy-формулы
    `points * max(1, factor)`).
  - `be_points` — это **резервуар активных уровней технологий**
    (legacy: `Mission.class.php:1675`, `EventHandler:363/1156`,
    Java `Participant.java:963` для начисления и
    `Participant.java:524-525` для применения в бою).

    **Полная картина legacy-механики**:
    1. **Накопление** — `be_points += experience` при бое
       (Java `Participant.java:963`), столько же сколько `e_points`.
       После боя у обоих сторон оба поля растут одинаково.
    2. **Использование** — при отправке атаки игрок выбирает
       `attack_lvl_<tech_id>` от `-K` до `+K`, где
       `K = min(20, floor(be_points / 100))`. Списывается
       `sum(|levels|) * 100` из be_points. Применимо к 5 техам:
       `gun`, `shield_weapon`, `shell_weapon`, `ballistics`,
       `masking`. В бою: `unit.attack *= (1 + lvl/10)` для GUN-техов,
       `unit.shield *= (1 + lvl/10)` для SHIELD-техов и т.д.
    3. **Возврат** — при отмене (cancel) события атаки:
       be_points возвращаются обратно (`EventHandler:1156`).

    **Скоуп для части 17 (план 72.1) — только накопление**:
    1. ✅ миграция БД (`be_points numeric NOT NULL DEFAULT 0`,
       уже сделано в 0082_users_legacy_extra_points.sql),
    2. ✅ поле в `/api/me` как `accumulated_experience` (уже отдаётся,
       значение 0 пока механики накопления нет),
    3. ⏳ **накопление**: hook в `internal/fleet/attack.go:734` рядом
       с `e_points += atkExp` — добавить аналогичный update
       `be_points += atkExp`. Так поле начинает наполняться при
       каждом бое и отображается на MainScreen.

    **Out-of-scope части 17 — отдельный план 73 «be_points use в бою»**:
    - UI в attack-mission form (выбор `attack_lvl_<tech>` от -K до +K),
    - валидация `sum(|lvl|) ≤ exp_active`,
    - списание/возврат be_points при создании/cancel события,
    - интеграция в `internal/battle/` (применение levels в формуле боя),
    - тесты battle-sim (баланс не сместился).

    Это сознательное **частичное прод-внедрение** (накопление без use):
    запись в `docs/simplifications.md` обязательна (п.11). Поле наполняется
    и видно на UI как «накопленный опыт», но пока не приносит in-game
    эффекта — игрок копит впрок, а реализация use-flow придёт в плане 73.
  - `of_level` + `of_points` + `need_points` — система **уровня
    Шахтёра** (legacy: `Functions.inc.php:1642-1665`,
    `NS.class.php:1218`). Формула:
    `need_points = (level<1) ? 100 : round(pow(1.5, level-1)*200)`.
    Эффект: бонус к скорости добычи. Требует:
    1. миграция БД,
    2. hook в mining tick (увеличение `of_points` при добыче),
    3. функция `recalcMinerLevel(of_points) → (of_level, need_points)`,
    4. интеграция в формулу production rate,
    5. поле в `/api/me`.
  - `dm_points` — derived-метрика (legacy:
    `Functions.inc.php:1432-1434`):
    `dm_points = POW(GREATEST(LEAST(e_points,100),
       LEAST(e_points, POW(points/4000,1.1)+e_points/100))
       * points / POW(GREATEST(1,max_points),0.9), 0.5)*100`.
    Обновляется в score-цикле (`internal/score`).
    Требует:
    1. миграция БД,
    2. SQL-формула в `score.RecalcUser`,
    3. поле в `/api/me`.
  - `reseachVirtLab` — это **уровень research-технологии Intergalactic
    Research Network**, рассчитывается на лету из лабораторий всех
    планет (legacy: `Planet.class.php:806-835`). НЕ хранимое поле.
    У нас в `internal/research` нужно проверить наличие
    соответствующей технологии и реализовать computed-getter.

  Каждый из этих 4 пунктов — фактически отдельная подзадача
  части 17. Объём — несколько дней работы (миграции, формулы,
  тесты, интеграция).
- **Online counter — отложить** (out-of-scope части 17). Legacy
  показывает `usersOnline` в `before_content.tpl`. Технически требует
  трекинга активных сессий в Redis или таблицы `user_sessions`. В origin
  пока не показываем.
- **Asset-картинки — копируем для темы `standard`**. В legacy много
  тем (`templates/standard/`, `templates/azure/`, ...). Берём только
  `standard` — это базовый дизайн legacy-PHP. Картинки планет/лун/
  кнопок копируем из `projects/game-legacy-php/public/images/...` в
  `projects/game-nova/frontends/origin/public/images/...`.
- **Картинки артефактов — должен быть код рендера в legacy**. При
  работе над частью 17 проверить `projects/game-legacy-php/src/` на
  предмет существующего рендера артефактов (вероятно
  `templates/standard/artefact*.tpl` + соответствующий controller).
  Не пилить с нуля — портировать готовое.

Требует расширения backend `/api/me` и `/api/planets` или нового endpoint:
- Военный опыт `e_points` (legacy) → `combat_experience` (origin DTO).
  Добавить в game-nova `users` миграцией (NOT NULL DEFAULT 0) и в
  `/api/me`. Воркер scoring уже считает — проверить, либо доавтоматить.
- Накопленный опыт `be_points` (legacy) → `accumulated_experience`.
  То же.
- Очки + ранг (`points` + `RANK_OF_USERS`) — в game-nova `users.points`
  уже считается воркером (см. `internal/score/`), но в `/api/me`
  не отдаётся. Расширить + добавить `rank: number` (позиция в
  highscore).
- Шахтёр (offLevel) / offPoints / need_points — поля профессии,
  в нашем `internal/profession/` уже есть формула, нужно отдать
  через `/api/me/profession` или вложенно в `/api/me`.
- Уровень межгалактических исследований (`reseachVirtLab` в legacy) —
  research уровень специфический. Проверить `internal/research/`
  catalog: если технология есть — отдать как поле; если нет —
  добавить или явно out-of-scope с обоснованием.

Требует frontend-asset-маппинг (без backend-правок):
- Картинки планет / Луна / planetMainSelection (3-колонный блок) —
  `planet_type` уже в API; нужен asset-mapping
  `planet_type → /images/planets/<X>.jpg` и копия PNG-файлов из
  `projects/game-legacy-php/public/images/planets/` (только тема
  `standard`) в `projects/game-nova/frontends/origin/public/images/planets/`.
- Картинки артефактов — портировать legacy-рендер (см. решение
  пользователя выше). Проверить `templates/standard/` и controllers
  в legacy-PHP.

**Pixel-perfect-визуал (часть 18 серии)** — стили, цвета, отступы,
шрифты должны зеркалить legacy CSS (см. `projects/game-legacy-php/
public/css/`). Соответствует требованию п.8 («pixel-perfect = и
стили, и раскраска»). Отдельная часть, потому что требует
кардинальной переработки CSS origin'а.

### 17-18. ✅ pixel-perfect Part 17-18: статистика, картинки, layout

**Часть 17** (мнемоника: stats + images + artefacts):
- `/api/me` расширен: `be_points` → `accumulated_experience`,
  `of_level`/`of_points` → `miner_level`/`miner_points`/`miner_need_points`,
  `dm_points`, `intergalactic_research_level`, `battles`, `rank`.
- БД миграция `0082_users_legacy_extra_points.sql`:
  `be_points`, `of_points`, `of_level`, `dm_points` (NOT NULL DEFAULT 0).
- Мнемоника шахтёра: `internal/miner/` — `LevelUp`, `NeedPoints`.
- Mining tick: `applyMinerTick` в `planet/miner_tick.go`.
- dm_points: `score.CalcDmPoints` + интеграция в `score.RecalcUser`.
- be_points: накопление в `fleet/attack.go` вместе с e_points.
- Картинки планет: `src/lib/planet-image.ts` (FNV-1a hash → детерм. выбор файла).
- Картинки артефактов: `ArtefactsScreen.tsx`, `ArtefactInfoScreen.tsx`
  с tri-fallback gif→png→usable_artefact.gif.
- Assets: 61 JPEG планет (full + small) + 124 GIF/PNG зданий/артефактов.

**Часть 18** (мнемоника: layout + images + rank):
- `PlanetsList`: иконки 60×60 / лун 20×20 из `planets/small/`.
- `MainScreen.PlanetSidebar`: `ul` → `itable` с иконками 89×89, имя + «нет заданий».
- Луна: 50×50 px (было maxWidth:200).
- Планет-картинка: 200×200 + ссылка `/constructions`.
- Военный опыт → ссылка `/ranking?sort=e_points`.
- Очки → ссылка `/ranking` + «rank из total_users» (шаблон `rankOfUsers`).
- `/api/me` добавлено поле `total_users` (COUNT активных игроков).
- i18n: `rankOfUsers` мигрирован `{@var}` → `{{var}}`, добавлен `noTasks`.
- CSS: `ntable` — `margin: auto`; `#topHeader li` — `float:left`;
  `#content` — скорректированы отступы; `#planets .cur-planet/cur-moon` — bg-спрайты;
  исправлены несуществующие `--color-*` → `--c-*`.

**Изменённые файлы (части 17-18)**:
- `migrations/0082_users_legacy_extra_points.sql` (новый).
- `backend/internal/auth/handler.go` — `/api/me` расширен, `total_users`.
- `backend/internal/miner/` (новый пакет), `backend/internal/planet/miner_tick.go`.
- `backend/internal/score/service.go` — `CalcDmPoints` + dm_points в UPDATE.
- `backend/internal/fleet/attack.go` — `be_points` накопление.
- `api/openapi.yaml` — MeInfo schema расширена.
- `configs/i18n/ru.yml`, `en.yml` — новые ключи.
- `frontends/origin/src/api/me.ts` — тип `MeInfo` расширен + `total_users`.
- `frontends/origin/src/lib/planet-image.ts` (новый).
- `frontends/origin/src/features/main/MainScreen.tsx` — pixel-perfect.
- `frontends/origin/src/features/artefacts/ArtefactsScreen.tsx`,
  `ArtefactInfoScreen.tsx` — картинки с fallback.
- `frontends/origin/src/features/common/artefact-catalog.ts` — `image` field.
- `frontends/origin/src/layout/PlanetsList.tsx` — иконки планет/лун.
- `frontends/origin/src/styles/base.css`, `layout.css`, `screens.css` — CSS fixes.
- `public/assets/origin/images/planets/` — 61 full + 60 small JPEG.
- `public/assets/origin/images/buildings/` — 124 GIF/PNG.

#### Детали реализации

**API в origin** (`projects/game-nova/frontends/origin/src/api/`):
- Расширён тип `Planet` (`types.ts`): добавлены `diameter`, `used_fields`,
  `max_fields`, `planet_type`, `temp_min`, `temp_max`, `*_per_sec`,
  `*_cap`, `energy_prod`, `energy_cons`, `energy_remaining`. Все
  значения уже возвращает `/api/planets`, просто не было в типе.
- Создан `me.ts` с `fetchMe(): Promise<MeInfo>` (user_id, username,
  profession, credit, roles).
- Добавлен `QK.me()` в `query-keys.ts`.

**i18n**:
- `main.planetOccupiedFields` мигрирован с legacy-формата `{@var}` →
  наш стандарт `{{var}}` (ru.yml и en.yml). Никто не использовал
  старый формат, проверено grep'ом.
- Новый ключ `overview.storageLabel` = «Склад:» (в legacy
  `layout.tpl` это строка была захардкожена без i18n — техдолг,
  устранили).
- Профессия переиспользует существующий `global.menuProfession`
  (то же что и в левом меню) — соответствует legacy `MENU_PROFESSION`.

**TopHeader.tsx** — pixel-perfect зеркало legacy `before_content.tpl`:
- 5 колонок ресурсов с подписями + значение + Склад/cap.
- Энергия в формате `remaining (prod)` как в legacy.
- Универс «Oxsar Classic», username, lang-select (RU disabled),
  кнопка «Выйти».

**MainScreen.tsx** — pixel-perfect зеркало legacy `main.tpl`:
- Заголовок `<th>` с именем, координатами и `(username)`.
- Строки: Главная планета, Профессия (link), Серверное время,
  Новые сообщения (если есть), События/Fleet missions, Диаметр
  (с `planetOccupiedFields` плейсхолдером), Температура, Координаты.

**Изменённые файлы (часть 16, в работе)**:
- `projects/game-nova/frontends/origin/src/api/types.ts` — расширен Planet.
- `projects/game-nova/frontends/origin/src/api/me.ts` — новый.
- `projects/game-nova/frontends/origin/src/api/query-keys.ts` — добавлен `me()`.
- `projects/game-nova/frontends/origin/src/layout/TopHeader.tsx` — переписан.
- `projects/game-nova/frontends/origin/src/features/main/MainScreen.tsx` — переписан.
- `projects/game-nova/configs/i18n/ru.yml` — миграция `planetOccupiedFields` + новый `storageLabel`.
- `projects/game-nova/configs/i18n/en.yml` — то же.

**Сделано финально**:
- Все строки в `TopHeader.tsx` и `MainScreen.tsx` через `t('group','key')`.
  Захардкоженных русских/английских слов в JSX нет.
- `overview.storageLabel` добавлен в обоих ru.yml и en.yml.
- Backend rebuild → `/api/i18n/ru` отдаёт все 10 нужных ключей.
- Vite HMR подхватил без ошибок.

**Использованные i18n-ключи** (все существовали кроме одного нового):
- `overview.resMetalLabel` / `resSiliconLabel` / `resHydrogenLabel` —
  существовали.
- `overview.storageLabel` — **новый**, добавлен в ru.yml и en.yml.
- `overview.diameterKm` / `planetDiameter` / `planetTemp` — существовали.
- `global.energy` / `credits` / `creditPay` / `btnLogout` /
  `menuProfession` — существовали.
- `main.curHomePlanet` / `serverTime` / `events` / `position` —
  существовали.
- `main.planetOccupiedFields` — существовал в legacy-формате `{@var}`,
  **мигрирован на `{{used}}` / `{{max}}`** (никем не использовался,
  безопасно).

## Out of scope (на будущее)

- Portal «Играть» → handoff: дописать `GET /api/universes/{slug}/join`
  в portal-backend, переключить `HomePage.tsx` с прямого `<a href>` на
  fetch+redirect с `?code=`. В dev обходится ручным логином на
  game-frontend.
- `DELETE /auth/users/me` → `DELETE /auth/me` (REST-симметрия).
- `identity-cli grant-role` (план 53e) — пока вручную SQL в `user_roles`.
- `usePolling: true` в `server.watch` для nova/portal vite.config'ов
  (HMR на Windows). Нагружает CPU, оставлено как trade-off.
- DEATHMATCH-режим — 11 переключателей фич (см. часть 11 +
  `docs/simplifications.md` `[config] DEATHMATCH-режим`). Требует
  детального аудита каждой точки интеграции в Go-коде и тестов.
  Не реализован в плане 72.1.
- Поиск планет в ±N систем для экспедиций (кольцевая логика
  «соседние системы» из legacy `Expedition.class.php:1007-1008`).
- Возможные дополнительные места валидации координат в
  `internal/fleet/moon_destruction.go`,
  `internal/origin/alien/handlers.go` — нужно проверить отдельно.
- Генерация compose-секций uni0N + env `VITE_UNIVERSE_PORT_uni0N` для
  portal-frontend из `configs/universes.yaml` (см. часть 13). Сейчас
  ручное согласование 3 мест при добавлении вселенной.

## Dev-инструменты: скриншоты и сравнение (pixel-perfect)

### Логины и пароли

| Сервис | URL | login | password |
|--------|-----|-------|----------|
| Legacy oxsar2 (PHP) | http://localhost:8092 | test | quoYaMe1wHo4xaci |
| Identity service | http://localhost:9000 | test | DevPass123 |
| Origin game frontend | http://localhost:5176 | — (через identity) | — |

**Все тестовые пользователи identity** (пароль одинаковый `DevPass123`):
`diffuser`, `player1`, `support1`, `mod1`, `admin1`, `billing1`, `super1`, `test`

### Как залогиниться в origin-фронт (5176) через Playwright

Origin-фронт не имеет собственного login-экрана — при отсутствии токена
`AuthGate` делает `window.location.replace('http://localhost:5174')` (портал).

Способ: использовать `addInitScript` для записи токена в localStorage
**до** React-инициализации:

```js
// 1. Получить токен через identity service
const raw = execSync(`curl -s -X POST http://localhost:9000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"login":"test","password":"DevPass123"}'`);
const { access_token, refresh_token, user } = JSON.parse(raw);

// 2. Инжектировать в localStorage ДО загрузки страницы
await ctx.addInitScript(({ access, refresh, userId }) => {
  localStorage.setItem('oxsar-origin-auth',
    JSON.stringify({ v: 1, access, refresh, userId }));
}, { access: access_token, refresh: refresh_token, userId: user.id });

// 3. Перейти на игровую страницу
await page.goto('http://localhost:5176', { waitUntil: 'networkidle' });
```

Формат хранения токена (ключ `oxsar-origin-auth`, version `v: 1`):
```json
{ "v": 1, "access": "<JWT>", "refresh": "<refresh_JWT>", "userId": "<uuid>" }
```

### Как залогиниться в legacy (8092) через Playwright

Использовать `dev-login.php` — ставит cookie `oxsar-jwt` с `alg=none`
и редиректит на `?go=Main`:

```js
await page.goto('http://localhost:8092/dev-login.php');
await page.waitForTimeout(2000);
```

### 19. ✅ Новые экраны из legacy-PHP: Resource / Defense / Disassemble / Stock

**Задача**: перенести недостающие страницы из `projects/game-legacy-php/`
в origin-фронт (http://localhost:5176). Роуты /resource, /defense, /disassemble,
/stock ранее вели на Navigate-заглушки (→ / или → /resource-market).

**Что сделано**:

1. **ResourceScreen** (`features/resource/ResourceScreen.tsx`) — экран
   производства ресурсов. Pixel-perfect клон `Resource.class.php`:
   - Таблица зданий: название/уровень/металл/кремний/водород в час/потребление энергии/фактор.
   - Строки «Базовое производство», «Склад», «Итого в час».
   - Сводка по суткам и неделе.
   - Поле ввода factor (0-100%) для зданий с `allow_factor=true` + кнопка «Сохранить».
   - Endpoints: `GET /api/planets/{id}/resource-report`, `POST /api/planets/{id}/resource-update`.

2. **DefenseScreen** (`features/defense/DefenseScreen.tsx`) — строительство
   обороны. Клон `Defense.class.php`:
   - Очередь строительства (если есть).
   - Таблица оборонительных юнитов с input + кнопка «Построить».
   - Использует shipyard endpoint (POST /api/planets/{id}/shipyard).

3. **DisassembleScreen** (`features/disassemble/DisassembleScreen.tsx`) —
   утилизация кораблей. Клон `Repair.class.php` (!isRepair mode):
   - Список кораблей на планете с радио-выбором юнита.
   - Поле количества (ограничено наличием на складе).
   - Активная очередь утилизации.
   - Endpoints: `GET /api/planets/{id}/shipyard/inventory`, `GET /api/planets/{id}/repair/queue`, `POST /api/planets/{id}/repair/disassemble`.

4. **StockScreen** (`features/stock/StockScreen.tsx`) — биржа артефактов.
   Клон `Stock.class.php`:
   - Таблица активных лотов: артефакт/количество/цена/цена-за-штуку/продавец/срок.
   - Cursor-based пагинация.
   - Endpoint: `GET /api/exchange/lots`.
   - Кнопка «Купить» заблокирована (покупка требует отдельного endpoint'а `POST /api/exchange/lots/{id}/buy`).

**Новые API-файлы**:
- `api/resource.ts` — `fetchResourceReport`, `updateResourceFactors`.
- `api/repair.ts` — `fetchRepairQueue`, `fetchDamagedShips`, `disassembleUnits`, `repairUnits`.
- `api/exchange.ts` — `fetchExchangeLots`.

**Новые типы** в `api/types.ts`:
- `ResourceBuilding`, `ResourceReport` — для resource-report endpoint.
- `RepairQueueItem` — для repair queue.
- `ExchangeLot`, `ExchangeLotsResult` — для биржи артефактов.

**Новые query-keys** в `api/query-keys.ts`:
- `resourceReport`, `repairQueue`, `repairDamaged`, `exchangeLots`.

**router.tsx**: Navigate-заглушки для /resource, /defense, /disassemble, /stock
заменены на настоящие компоненты.

**Упрощения** (зафиксировано как out-of-scope):
- Кнопка «Купить» в StockScreen disabled — нет `POST /api/exchange/lots/{id}/buy` в backend.
- В DefenseScreen/DisassembleScreen юнит-названия берутся из catalog i18n-ключей напрямую
  (формат `group.key`), а не через `useTranslation()` — в этих экранах все строки
  статические из каталога, не нужен i18n-контекст.

**Изменённые файлы (часть 19)**:
- `src/features/resource/ResourceScreen.tsx` (новый).
- `src/features/defense/DefenseScreen.tsx` (новый).
- `src/features/disassemble/DisassembleScreen.tsx` (новый).
- `src/features/stock/StockScreen.tsx` (новый).
- `src/api/resource.ts` (новый).
- `src/api/repair.ts` (новый).
- `src/api/exchange.ts` (новый).
- `src/api/types.ts` — добавлены ResourceBuilding, ResourceReport, RepairQueueItem, ExchangeLot, ExchangeLotsResult.
- `src/api/query-keys.ts` — добавлены resourceReport, repairQueue, repairDamaged, exchangeLots.
- `src/router.tsx` — Navigate-заглушки заменены на экраны + обновлён комментарий.

### Скрипт для скриншотов

Скрипт находится в `tests/e2e/origin-frontend/screenshot.mjs`.
Запуск: `node screenshot.mjs` из директории `tests/e2e/origin-frontend/`.
Сохраняет в `C:/Users/Евгений/OneDrive/Рабочий стол/`:
- `origin_game.png` / `origin_menu.png` / `origin_header.png` / `origin_full.png`
- `legacy_game.png` / `legacy_menu.png` / `legacy_header.png` / `legacy_full.png`

Доп. скрипт `research-compare.mjs` сохраняет:
- `origin_research.png` (origin /research, fullPage)
- `legacy_research.png` (legacy /game.php?go=Research, fullPage)

## Часть 20: Финализация Research/Constructions/Shipyard/Defense (без упрощений)

**Дата старта**: 2026-04-29.

**Контекст**: при сравнении скриншотов /research origin vs legacy
выявлено, что текущая реализация ResearchScreen — заглушка, а не
pixel-perfect клон research.tpl. Аналогичные дыры в Constructions
(уровни/cost/время отсутствуют), Shipyard/Defense (нет cost preview),
Disassemble.

Принят принцип «без упрощений / как для прода» (см. п.11 общих
правил). Запись в plan, чтобы скоуп был фиксирован и сравнение
с legacy воспроизводимо.

### 20.0 Таблица статусов экранов (ОБЯЗАТЕЛЬНО для всех агентов)

**Это обязательное требование плана 72.1.** Любой агент, работающий
над финализацией экрана origin-фронта, ОБЯЗАН обновить эту таблицу
в том же коммите, в котором меняет экран. Без обновления таблицы
коммит считается неполным.

**Колонки**:
- **Classic**: путь URL на origin-фронте (Oxsar Classic), например `/research`.
  Базовый URL — `http://localhost:5176`.
- **Legacy-PHP**: путь URL в legacy, всегда формат `?go=ClassName`
  (например `?go=Research`). Базовый URL — `http://localhost:8092/game.php`.
- **Статус**: `not-started` / `in-progress` / `pixel-perfect` / `blocked`.
- **Дата статуса**: ISO `YYYY-MM-DD` — когда статус последний раз менялся.
- **% pixel-perfect**: оценка совпадения с legacy при последнем
  сравнении скриншотов (0-100). При `not-started` ставить `0`,
  `blocked` — последняя оценка перед блокировкой.
- **Коммит**: short-hash последнего коммита, изменившего этот экран.
  Используется для быстрого `git show <hash>` чтобы посмотреть что
  менялось. Для `not-started` ставить `—`.

**Семантика статусов**:
- `not-started` — ещё не начат, либо текущая реализация неактуальна.
- `in-progress` — работа идёт или начата, но не дошла до 95%.
- `pixel-perfect` — финализирован, скриншот origin vs legacy 95%+.
- `blocked` — блокируется внешней зависимостью (нет endpoint'а,
  нет данных, baseline отличается).

**Правила обновления**:
1. Перед началом работы над экраном — поменять статус на `in-progress`,
   обновить дату.
2. После каждого коммита по экрану — обновить % и дату, **проставить
   short-hash коммита в колонку «Коммит»**.
3. При достижении 95%+ — статус `pixel-perfect` и дата фиксируются.
4. Если выясняется блокер — статус `blocked` (детали в коммите/тикете).
5. Не удалять строки, только обновлять.

**Перечитывание плана (обязательное требование)**:

Этот план — единый источник истины по работе над финализацией.
Поскольку требования и таблица статусов меняются, агент ОБЯЗАН
перечитывать план в следующих ситуациях:

1. **После каждого коммита**, который касается экрана из таблицы 20.0.
   Пользователь мог добавить новые требования / правила / экраны
   между коммитами.
2. **Перед началом работы над новым экраном** — заново прочитать
   разделы 20.0 (таблица), 20.8 (workflow), 11.1 (чеклист
   высокой детализации). Они могут содержать обновления, которых
   не было в начале сессии.
3. **При смене сессии Claude / агента** — план читается полностью
   с раздела «Часть 20» до конца. Не доверять кешу.
4. **При получении правки пользователя «обнови план», «добавь в план»** —
   немедленно прочитать соответствующий раздел перед правкой.

Способ перечитывания:
```bash
# Прочитать только раздел 20.0 (таблица) — типично достаточно:
sed -n '/^### 20.0/,/^### 20.1/p' docs/plans/72.1-post-remaster-stabilization.md

# Полностью часть 20:
sed -n '/^## Часть 20/,$p' docs/plans/72.1-post-remaster-stabilization.md
```

Или через инструмент Read с offset/limit на нужные строки.

**Запрещено**: работать «по памяти» с прошлой сессии. Требования
плана обновляются часто, и работа без перечитывания приводит к
коммитам, нарушающим текущие правила.

| Classic | Legacy-PHP | Статус | Дата статуса | % pixel-perfect | Коммит |
|---------|------------|--------|--------------|-----------------|--------|
| `/` | `?go=Main` | in-progress | 2026-04-29 | 80 | 8657a4d4 |
| `/resource` | `?go=Resource` | in-progress | 2026-04-29 | 85 | 70298ad3 |
| `/constructions` | `?go=Constructions` | in-progress | 2026-04-29 | 85 | 9d507573 |
| `/research` | `?go=Research` | in-progress | 2026-04-29 | 85 | 9d507573 |
| `/shipyard` | `?go=Shipyard` | in-progress | 2026-04-30 | 90 | 0b419044 |
| `/defense` | `?go=Defense` | in-progress | 2026-04-30 | 90 | 0b419044 |
| `/mission` | `?go=Mission` | not-started | 2026-04-29 | 0 | — |
| `/disassemble` | `?go=Repair` (disassemble) | in-progress | 2026-04-29 | 70 | eca5c24e |
| `/repair` | `?go=Repair` | not-started | 2026-04-29 | 0 | — |
| `/stock` | `?go=Stock` | in-progress | 2026-04-29 | 70 | eca5c24e |
| `/galaxy` | `?go=Galaxy` | not-started | 2026-04-29 | 0 | — |
| `/empire` | `?go=Empire` | not-started | 2026-04-29 | 0 | — |
| `/techtree` | `?go=Techtree` | not-started | 2026-04-29 | 0 | — |
| `/profession` | `?go=Profession` | not-started | 2026-04-29 | 0 | — |
| `/ranking` | `?go=Ranking` | not-started | 2026-04-29 | 0 | — |
| `/records` | `?go=Records` | not-started | 2026-04-29 | 0 | — |
| `/battlestats` | `?go=Battlestats` | not-started | 2026-04-29 | 0 | — |
| `/artefacts` | `?go=Artefacts` | not-started | 2026-04-29 | 0 | — |
| `/resource-market` | `?go=Exchange` | not-started | 2026-04-29 | 0 | — |
| `/market` | `?go=ArtefactMarket` | not-started | 2026-04-29 | 0 | — |
| `/payment` | `?go=Payment` | not-started | 2026-04-29 | 0 | — |
| `/chat` | `?go=Chat` | not-started | 2026-04-29 | 0 | — |
| `/msg` | `?go=MSG` | not-started | 2026-04-29 | 0 | — |
| `/notepad` | `?go=Notepad` | not-started | 2026-04-29 | 0 | — |
| `/alliance` | `?go=Alliance` | not-started | 2026-04-29 | 0 | — |
| `/friends` | `?go=Friends` | not-started | 2026-04-29 | 0 | — |
| `/search` | `?go=Search` | not-started | 2026-04-29 | 0 | — |
| `/settings` | `?go=Preferences` | not-started | 2026-04-29 | 0 | — |
| `/user-agreement` | `?go=UserAgreement` | not-started | 2026-04-29 | 0 | — |
| `/support` | `?go=Support` | not-started | 2026-04-29 | 0 | — |
| `/tools/tech-calc` | `?go=AdvTechCalculator` | not-started | 2026-04-29 | 0 | — |
| `/logout` | `?go=Logout` | n/a | 2026-04-29 | n/a | — |
| `TopHeader` (layout) | layout.tpl `#topHeader` | in-progress | 2026-04-29 | 90 | 8657a4d4 |
| `LeftMenu` (layout) | layout.tpl `#leftMenu` | in-progress | 2026-04-29 | 90 | 07660117 |
| `PlanetsList` (layout) | layout.tpl `#planets` | in-progress | 2026-04-29 | 90 | e6b508e3 |
| `Footer` (layout) | layout.tpl `.oxsar-footer` | in-progress | 2026-04-29 | 90 | e6b508e3 |

**Цель плана 72.1 ч.20**: довести все экраны до `pixel-perfect`
(95%+) или явного `blocked` с заметкой.

### 20.1 ResearchScreen (приоритет)

Что сейчас не так:
- CATALOG.research содержит только 10 ID — в legacy ~25 технологий.
- Иконки `/assets/origin/images/units/{id}.gif` не отображаются
  (broken-image hidden) — файлы либо отсутствуют, либо неправильный
  путь.
- Нет блока стоимости ресурсов (`required_res_table` в legacy):
  металл/кремний/водород + время постройки.
- Нет уровня справа (legacy: `Уровень: N` через `<span float:right>`).
- Кнопка просто «Изучить» — в legacy `Исследовать (через Xм Yс)`.

Что делаем:
1. Расширить CATALOG.research — все ID из `configs/research.yml`,
   с правильными `info.*` ключами i18n.
2. Проверить наличие файлов иконок в
   `projects/game-nova/frontends/origin/public/assets/origin/images/units/`.
   Недостающие — скопировать из legacy
   `projects/game-legacy-php/public/images/` (имена legacy могут
   отличаться от {id}.gif — карта соответствий).
3. Использовать `GET /api/units/catalog/{type}` (preview cost/time
   по уровням 1, 5, 10, 20, 30, 40) — endpoint существует с плана
   72 Ф.4 S-019. Запрос делается per-tech через query на следующий
   уровень.
4. Cost preview как в legacy `required_res_table.tpl`: 4 строки
   (металл/кремний/водород/время), цвет `false` если ресурс
   недостаточен (сравнение с `planet.metal/silicon/hydrogen`).
5. Кнопка «Исследовать» с временем (если `researchLab` уровень есть
   — формула `time = base / (researchLab + 1)`); disabled если
   очередь не пуста или ресурсов нет.
6. i18n обновить для всех технологий — ключи `info.*` уже есть в
   ru.yml, проверить en.yml на полноту, дописать недостающее.

### 20.2 ConstructionsScreen

Те же дыры, что в Research:
- Уровень показывается, но без bonus `(+N)`/`(-N)` от артефактов.
- Cost preview отсутствует.
- Время постройки не показано.

Использовать `GET /api/buildings/catalog/{type}` аналогично п.20.1.

### 20.3 Shipyard / Defense

Cost preview для `count=1` юнита, total = base * count. Время
постройки = `time_base / (shipyard_level + nano_factory_level)` —
формула из `economy/build_time.go`.

### 20.4 DisassembleScreen / RepairScreen / StockScreen

Уже на pixel-perfect уровне (часть 19), доводим только если
выявятся отличия при сравнении скриншотов.

### 20.5 Сравнительный тест

Расширить `tests/e2e/origin-frontend/research-compare.mjs` до
полного набора экранов: research / constructions / shipyard /
defense / disassemble / stock / resource. Сохранять в
`C:/Users/Евгений/OneDrive/Рабочий стол/compare-{screen}.png`
(origin + legacy в один файл через chromium screenshot composit
— или два рядом стоящих файла).

### 20.6 Принципы (напоминание)

Применяется п.11 «Prod-quality, без упрощений»:
- Никаких TODO-заглушек.
- i18n полный (ru + en).
- OpenAPI обновляется в том же коммите.
- `schema.d.ts` пересобирается.
- Любое отклонение → запись в `docs/simplifications.md` с планом
  возврата.

### 20.7 Карта зависимостей

```
[catalog endpoints (Ф.4)] → [CATALOG расширение] → [Иконки копирование]
                                ↓
                  [ResearchScreen v2 + cost preview]
                                ↓
                  [ConstructionsScreen v2]
                                ↓
                  [Shipyard/Defense v2]
                                ↓
                  [Скриншот-тест по всем экранам]
```

### 20.8 Workflow pixel-perfect-сравнения экранов (для других агентов)

Этот раздел документирует **проверенный** воркфлоу, по которому
финализируются экраны origin-фронта в плане 72.1. Используется как
эталон для других агентов, продолжающих эту работу.

#### Папка для скриншотов

`tests/e2e/origin-frontend/screenshots-{plan-id}/` — например
`screenshots-72.1/`. Имя папки **обязательно** содержит ID плана,
чтобы артефакты разных планов не смешивались.

- Папка добавлена в `.gitignore` (`screenshots-*/*.png`) — скриншоты
  это локальные артефакты, не код, не коммитятся.
- В каждой папке держим `.gitkeep` чтобы Git её зафиксировал
  как существующую (но пустую).
- Все mjs-скрипты сравнения используют:
  ```js
  const OUT = new URL('./screenshots-72.1', import.meta.url).pathname.replace(/^\//, '');
  ```

#### Алгоритм работы над экраном (12 шагов)

**Цель**: pixel-perfect клон legacy-экрана с принципом «без упрощений».
Достигаемая точность: 80-90% pixel-perfect (остальные 10-20% —
расхождение баланса/данных, не визуала).

**Шаг 1. Сравнить визуально текущее состояние**

```bash
# Сделать первичный скриншот текущей версии экрана:
cd tests/e2e/origin-frontend
node {screen}-compare.mjs   # если скрипта нет — создать на базе research-compare.mjs
```

Открыть `screenshots-72.1/origin_{screen}.png` и
`screenshots-72.1/legacy_{screen}.png` рядом. Записать список
конкретных отличий.

**Шаг 2. Прочитать legacy-шаблон**

`projects/game-legacy-php/src/templates/standard/{screen}.tpl` —
это эталон. Также:
- Class-файл `projects/game-legacy-php/src/game/page/{Screen}.class.php` —
  логика подготовки данных.
- `required_res_table.tpl` — типичный sub-шаблон стоимости.

**Шаг 3. Прочитать текущий React-компонент**

`projects/game-nova/frontends/origin/src/features/{screen}/`. Идентифицировать дыры:
- Что не отрисовывается (иконки/cost/время/уровень/описание).
- Что лишнее (заглушки, mock-данные, hardcoded значения).
- Какие i18n-ключи используются (часто не те — проверять группу).

**Шаг 4. Проверить иконки**

Лежат в `projects/game-nova/frontends/origin/public/assets/origin/images/units/`.
Источник — legacy `projects/game-legacy-php/public/images/buildings/std/`.

```bash
# Скопировать недостающие иконки одной командой:
cp -r projects/game-legacy-php/public/images/buildings/std/* \
      projects/game-nova/frontends/origin/public/assets/origin/images/units/
```

ВАЖНО: имена иконок в legacy НЕ совпадают с текущими `key` из `units.yml`:
- `metal_mine` (units.yml) ↔ `metalmine.gif` (legacy)
- `big_transporter` ↔ `large_transporter.gif`
- `heavy_fighter` ↔ `strong_fighter.gif`
- `star_destroyer` ↔ `frigate.gif`

Решение: в `CatalogEntry` есть поле `icon` отдельно от `i18n` —
маппинг ключ→файл вшит в каталог.

**Шаг 5. Расширить CATALOG**

`src/features/common/catalog.ts` — добавить недостающие ID, проверить
все три поля: `id`, `i18n`, `icon`. Не пропускать `moonOnly: true` для
лунных зданий (54, 55, 56, 57).

**Шаг 6. Backend — endpoint для cost+time**

Если в openapi нет агрегированного endpoint'а с `levels + cost + time`
для типа юнита — добавить:

- Service: `XxxCostsMap(levels) map[int]Cost` — стоимость след. уровня.
- Service: `XxxSecondsMap(...)` — время след. уровня (formula уже
  обычно есть в `economy/`).
- Handler: расширить response с `{costs, seconds, requirements_unmet}`.
- OpenAPI: добавить схему response.
- `npm run gen:api` в origin-фронте — пересобрать `schema.d.ts`.
- `docker compose -f deploy/docker-compose.yml build uni03-backend` —
  пересобрать образ. Просто `docker restart` НЕ обновит бинарник.
- `docker compose -f deploy/docker-compose.yml up -d uni03-backend` —
  поднять с новым образом.

Проверка через curl (логин в identity, получить planet, дернуть endpoint):

```bash
TOKEN=$(curl -s -X POST http://localhost:9000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"login":"test","password":"DevPass123"}' \
  | grep -o '"access_token":"[^"]*"' | cut -d'"' -f4)
PLANET=$(curl -s http://localhost:8083/api/planets \
  -H "Authorization: Bearer $TOKEN" \
  | grep -o '"id":"[^"]*"' | head -1 | cut -d'"' -f4)
curl -s "http://localhost:8083/api/planets/$PLANET/buildings" \
  -H "Authorization: Bearer $TOKEN" | head -c 500
```

**Шаг 7. Frontend API-клиент**

`src/api/{domain}.ts` — добавить fetcher + типы:
- `Xxx Cost { metal, silicon, hydrogen }`
- `RequirementUnmet { unit_id, required_level, actual_level }`
- `XxxOverview { levels, build_seconds, build_costs, requirements_unmet }`
- `fetchXxxOverview(planetId): Promise<XxxOverview>`

`src/api/query-keys.ts` — query-key для новых данных.

**Шаг 8. Перепиши экран pixel-perfect**

Структура `<table className="ntable">` с тремя колонками:

```tsx
<tr>
  <td width="1px" style={{ verticalAlign: 'top' }}>
    <img src={`/assets/origin/images/units/${entry.icon}.gif`}
         alt={t(group, key)}
         onError={(e) => { (e.target as HTMLImageElement).style.display = 'none'; }} />
  </td>
  <td style={{ verticalAlign: 'top' }}>
    <div style={{ width: '100%' }}>
      <span style={{ float: 'right' }}>{t('buildings', 'levelAbbr')} {level}</span>
      {t(group, key)}
    </div>
    {hasDesc && <div style={{ clear: 'both', fontSize: 'smaller' }}>{desc}</div>}
    {hasRequirements ? <RequirementsList /> : <RequiredResTable cost={...} seconds={...} />}
  </td>
  <td width="100px" align="center" style={{ verticalAlign: 'top' }}>
    {queueBusy ? (
      <span className="false">{t('buildings', 'buildingAtWork')}</span>
    ) : (
      <span className={enough ? 'true' : 'false'}>
        <input type="button" className="button"
               value={`${t('buildings', 'upgradeToLevel')} ${level + 1}`}
               disabled={!enough || queueBusy} />
      </span>
    )}
  </td>
</tr>
```

Использовать общий компонент `RequiredResTable` из
`src/features/common/RequiredResTable.tsx` для cost-таблицы.

**Шаг 9. Стили**

В `base.css` уже добавлены `.notavailable`, `.true`, `.false`,
`table.table_no_background`. Если нужны новые legacy-классы —
сверять с `projects/game-legacy-php/public/css/style.css`.

**Шаг 10. Type-check + restart**

```bash
cd projects/game-nova/frontends/origin && npx tsc --noEmit
docker restart deploy-uni03-frontend-1   # vite перечитает файлы
```

**Шаг 11. Сделать сравнительный скриншот**

```bash
cd tests/e2e/origin-frontend && node {screen}-compare.mjs
```

Открыть оба PNG в `screenshots-72.1/` и проверить визуально.
Частые проблемы:
- Иконки не загружаются → проверить `entry.icon` совпадает с именем
  файла в `public/assets/origin/images/units/`.
- Cost = 0 → backend не пересобран (docker compose build, не
  restart).
- Уровень 0 для существующих зданий → проверить `levels` в API.

**Шаг 11.1. Чеклист высокой детализации при сравнении скриншотов**

При сравнении origin vs legacy ОБЯЗАТЕЛЬНО проверить КАЖДЫЙ пункт.
Пользователь типично замечает мелочи, которые на первый взгляд
кажутся незначительными. Поверхностное сравнение «похоже» = провал.

#### Числа и форматирование
- [ ] Разделитель тысяч: точка `.` (legacy ru: `409.600`, не `409 600`).
- [ ] Десятичный разделитель: запятая `,` для дробных чисел.
- [ ] Округление: до целого для ресурсов, 1-2 знака для процентов.
- [ ] Минус: `-1.234`, не `–1.234` (обычный минус, не em-dash).

#### Кнопки действия
- [ ] Многострочные ли в legacy (например `Исследовать\nуровень 12`)?
- [ ] Цвет: зелёный `.true` если доступно, красный/серый `.false` если нет.
- [ ] Без рамки/фона у `.btn-link` (просто текст-ссылка) ИЛИ с рамкой
      `.button` (input-стиль) — смотри как именно в legacy.
- [ ] Текст кнопки: «Исследовать» vs «Изучить», «Построить уровень N+1»
      vs просто «Построить» (legacy показывает целевой уровень).
- [ ] Состояние «занято»: текст `BUILDING_AT_WORK`, цвет `.false`.

#### Уровни и мета-инфо
- [ ] Формат: «Уровень N» (полное слово) vs «Ур. N» (сокращение)?
- [ ] Bonus от артефактов: `Уровень 4 (-1)` или `(+2)` — есть ли это
      поле в backend? Если нет — записать в части плана.
- [ ] Расположение: float-right в первой строке.

#### Cost-таблица
- [ ] Метка ресурса (Металл/Кремний/Водород) — слева.
- [ ] Значение — справа от метки, выровнено или по центру.
- [ ] Скобка с нехваткой `(123.456)` в третьей колонке справа.
- [ ] Цвет: `.true` зелёный когда хватает, `.notavailable` красный
      когда нет.
- [ ] Время — отдельная строка «Время HH:MM:SS».
- [ ] Padding между колонками: примерно 16-20px.

#### Описание
- [ ] Видно полностью, не обрезано.
- [ ] `font-size: smaller` для описания.
- [ ] Между названием и описанием — `clear: both` чтобы float-right
      уровень не накладывался.

#### Иконки
- [ ] Размер: ~80×80 (legacy большие). Не 32×32, не растянуты.
- [ ] Корректный файл (см. карту icon-key в catalog.ts).
- [ ] `verticalAlign: top` — иконка вверху ячейки.

#### Layout
- [ ] Ширина первой колонки `width="1px"` (по содержимому = иконка).
- [ ] Третья колонка `width="100px"`.
- [ ] Промежутки между строками таблицы — как в legacy (нет лишнего
      `margin-bottom`).
- [ ] Шапка таблицы `<th colSpan="3">Заголовок</th>` с фоном
      `var(--c-th-bg)`.

#### Шапка страницы (#topHeader)
- [ ] Имя планеты + координаты `[1:1:1]` (одна пара скобок, не `[[`).
- [ ] Числа в шапке: ресурсы / cap (с точкой как разделитель).
- [ ] Энергия: формат `5.556 (11.023)` — текущая (резерв).
- [ ] Кнопка test/Выйти — есть ли в legacy? (нет → убрать).

#### Левое меню
- [ ] Иконки секций: `float: right` (legacy), не `float: left`.
- [ ] Текст исчезает при раскрытии секции (legacy menuSoH).
- [ ] Цвет фона секций: разные colorClass (menu2-min, menu3-min, ...).

#### Правый список планет
- [ ] Луна сразу под своей планетой (одинаковые galaxy/system/position).
- [ ] Луна 20×20, планета 60×60.
- [ ] Фон `cur-planet`/`cur-moon` на текущей.

#### Footer
- [ ] Структура `<div class="age-rating">12+</div><div class="legal-links">...</div>`.
- [ ] Ссылки: Оферта | Правила | Возврат | Конфиденциальность (порядок).
- [ ] Разделитель `<span class="sep">|</span>` между ссылками, не между 12+ и первой.
- [ ] Position fixed bottom.

#### Курсор
- [ ] `cursor: pointer` на меню-секциях (не `n-resize` или `default`).
- [ ] Текст НЕ contenteditable (мигающий курсор = бракован body или div).

#### Что делать с найденными отличиями

1. Записать **все** найденные отличия в одно сообщение пользователю
   до правок. Иначе теряется контекст.
2. Сгруппировать по категориям выше.
3. Для каждого отличия — сразу указать причину (CSS / data / структура HTML).
4. Чинить по одному, после каждого делать новый скриншот.
5. Если отличие требует backend-работы (поле в API, новый endpoint) —
   записать в `## Часть N` плана как отдельный sub-step.

**Запись «80% pixel-perfect» НЕ означает «готово».** Цель: 95%+ при
финализации экрана. Оставшиеся 5% — это objective baseline (данные,
которые невозможно воспроизвести на тестовом юзере без идентичных
артефактов и уровней зданий).

**Шаг 12. Обновить таблицу статусов экранов (раздел 20.0)**

ОБЯЗАТЕЛЬНО. В том же коммите, что и изменение экрана:
- Найти строку экрана в таблице 20.0.
- Обновить статус (`in-progress` → `pixel-perfect` если 95%+ достигнут).
- Обновить дату на сегодняшнюю (ISO YYYY-MM-DD).
- Обновить % pixel-perfect — реалистичная оценка по чеклисту 11.1.
- Колонка «Коммит» — будет заполнена short-hash в шаге 13 (после
  фактического `git commit`).

**Шаг 13. Коммит**

Conventional Commits, тип `feat:` для новых экранов, `fix:` для
доводок. В описании перечислить что добавлено в backend, frontend,
openapi отдельно.

В body коммита упомянуть таблицу 20.0:
```
- screens-status table: /research 70% → 85% (in-progress)
```

**После `git commit` — Шаг 13.1**: получить short-hash через
`git log --oneline -1`, записать его в колонку «Коммит» строки экрана,
сделать **второй небольшой коммит** только с обновлением таблицы
(или amend в первый, если ничего другого не меняли):

```bash
git log --oneline -1   # f7545c56b1 docs(plan-72.1)...
# Вписать f7545c56 в таблицу 20.0
git add docs/plans/72.1-post-remaster-stabilization.md
git commit -m "docs(plan-72.1): screens-status table /research → f7545c56"
```

**Шаг 13.2 — перечитать план**:

После каждого коммита перечитать раздел 20.0 плана. Между коммитами
пользователь мог:
- Добавить требование (новая колонка таблицы, новый шаг workflow,
  ужесточение чеклиста).
- Расширить таблицу (новый экран, новая категория).
- Поменять процент-цель pixel-perfect.

Игнорирование этих обновлений = коммит нарушает план = pre-commit
brake.

#### Локальные тестовые данные

- Identity URL: `http://localhost:9000`
- Origin frontend: `http://localhost:5176`
- Origin backend (uni03): `http://localhost:8083`
- Legacy PHP: `http://localhost:8092`
- Тестовый пользователь: `test` / `DevPass123` (identity-service)
- Legacy login без пароля: `http://localhost:8092/dev-login.php`

Скрипты сравнения уже умеют логиниться — переиспользовать.

#### Чеклист «без упрощений» (п.11)

Перед коммитом проверить:
- [ ] Нет `any`, `@ts-ignore`, hardcoded mock-данных.
- [ ] Все TODO-заглушки закрыты или вынесены в `simplifications.md`.
- [ ] OpenAPI обновлён в этом же коммите.
- [ ] `schema.d.ts` перегенерирован.
- [ ] i18n полный (ru используется через `t(group, key)`, en —
      проверить наличие ключей).
- [ ] Permission-checks в backend handler.
- [ ] Логи `slog` со всеми контекстными полями.
- [ ] Скриншот origin vs legacy визуально совпадает на 95%+ (см. чеклист 11.1).
- [ ] **Таблица статусов экранов 20.0 обновлена** (статус, дата, %).

#### Принцип «не упростил → не закоммитил»

Если по ходу выявилась дыра (например, нет endpoint'а для preview cost) —
**не** оставлять заглушку с `// TODO`. Варианты:
1. Дописать endpoint в backend (предпочтительно).
2. Если требует большой работы — выделить в отдельную итерацию
   и записать в `## Часть N` плана.
3. В крайнем случае — `docs/simplifications.md` запись с планом возврата.

Без записи отклонения недопустимы.

### 20.9 Backend bonus-уровень артефактов (added_level)

**Откуда**: legacy показывает `Уровень N (-2)` — это бонус от
артефактов (слот memory_module, artefact_packing_research и т.п.
прибавляют/вычитают уровни). Backend origin сейчас не возвращает
это поле через `/api/research` или `/api/planets/{id}/buildings`.

**Что нужно**:
- В service.go рядом с `Levels()` добавить `EffectiveLevels()` —
  с учётом активных артефактов на планете/у юзера.
- Вернуть как `added_levels: map[int]int` (offset, может быть отриц.).
- Фронт показывает `Уровень N` + `<span class="true">(+M)</span>` или
  `<span class="false">(M)</span>` если M отрицательный.

**Зависимость**: артефакты в backend уже есть (`/api/artefacts/active`).
Нужно вычислить эффект на уровни через registered effects из catalog.
